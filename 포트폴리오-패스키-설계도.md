# 내 소개 페이지에 패스키 달기 — 설계도

과제 1(`portfolio/`, 커밋 `cb23773`의 `index.html`)에 **비밀번호 없는 로그인(패스키/WebAuthn)**을
붙여, 나만 보는 자리를 새로 만들고 그 자리만 잠근다. "막았습니다"가 아니라 **막히는 장면**
(성공 요청과 거절 요청을 나란히)으로 증명한다.

> **이 과제는 새 프로젝트다.** 과제 6→7 때와 같은 규칙 — 결과물 주소·소스 폴더·백엔드
> 프로젝트를 전부 새로 판다. 다만 **소스 자체는 과제 1을 복제해 이어간다** — `portfolio`가
> 지금 gitlink로 남아 실제 파일이 워킹트리에 없으므로, 새 폴더 `portfolio-passkey/`에는
> 커밋 `cb23773`의 `index.html`·`po.md`를 그대로 꺼내 심고 그 위에 이어붙인다(T08-C11:
> "1번 과제로 만든 소개 페이지에 이어 붙였고, 1번의 공개 내용이 그대로 남아 있다").

---

## 0. 이 설계도의 기준

과제 카드 5개는 아래 원칙에서 따라 나온다. 구현 중 판단이 갈리면 이 표로 되돌아온다.

| # | 원칙 | 이 원칙이 막아주는 실패 |
|---|------|------------------------|
| 1 | **비밀번호를 아예 만들지 않는다.** 등록·로그인 어디에도 비밀번호 입력란이 없다. | "패스워드리스"를 표방하면서 몰래 비밀번호 필드를 하나 남겨두는 흔한 반쪽짜리 구현 |
| 2 | **개인키는 절대 기기 밖으로 나가지 않는다.** 서버가 받아 저장하는 것은 오직 공개키뿐이다 — WebAuthn 표준 자체가 이걸 강제한다. | 개인키가 네트워크를 타는 순간 그 인증은 더 이상 "패스키"가 아니다 |
| 3 | **화면에서 숨기는 것과 서버가 거절하는 것은 다르다.** 비공개 자료는 `display:none`이 아니라 **API가 401/403을 돌려주고, 애초에 초기 HTML 소스에 실리지 않아야** 한다. | 과제 7과 동일한 함정 — "목록에서만 안 보이게 했더니 view-source엔 다 있더라" |
| 4 | **행마다 주인은 클라이언트가 아니라 서버가 정한다.** 요청 본문에 남의 계정 id를 적어 보내도 서버는 세션이 가리키는 계정으로만 쓰고 읽는다. | 과제 7의 `stamp_owner` 원칙 재사용 — IDOR을 구조적으로 막는다 |
| 5 | **매번 새 챌린지, 한 번 쓰면 폐기.** 등록·로그인 요청마다 서버가 새 난수를 만들고, 검증 순간 그 챌린지를 `used_at`으로 소진 처리한다. | 같은 서명을 재전송하는 재생 공격(replay)을 막는다 — WebAuthn의 핵심 방어 |
| 6 | **다 막았다고 적지 않는다.** "⑥ 아직 못 막은 것"은 반드시 하나 이상, 구체적으로 채운다. | 카드 5 troubleshooting 원문 — 다 막았다고 적으면 오히려 재확인을 부른다 |

> **세션은 stateless 토큰(JWT)이 아니라 서버가 상태를 쥔 세션 테이블로 간다.**
> 과제 7에서 JWT가 로그아웃해도 서명이 만료 전까지 살아있는 문제를 `session_is_active()`로
> 사후에 땜질했다. 이번엔 처음부터 세션 id를 DB 행 하나에 대응시켜, 로그아웃 = 그 행 삭제 =
> 즉시 무효화가 되도록 설계한다(T08-C32·C33).

---

## 1. 무엇을 잠글지 먼저 가르기 (카드 1)

### 공개 영역 — 과제 1 그대로

`portfolio-passkey/index.html`은 과제 1의 히어로·About·공개 범위·Strengths·Proof 섹션을
**그대로** 옮긴다. 문구 하나 안 바꾼다 — T08-C11이 "1번의 공개 내용이 그대로 남아 있다"를
요구하기 때문이다.

### 비공개 영역 — 새로 추가

기존 `<footer>` 앞에 `🔒 나만 보는 자리`라는 새 섹션을 추가한다. 항목 3개(T08-C14):

| 항목 | 내용(전부 지어낸 것) |
|------|----------------------|
| 준비 중인 프로젝트 메모 | 아직 공개하지 않은 사이드 프로젝트 아이디어 메모 |
| 지원하려는 곳 목록 | 가상의 회사명 3~4곳 + 지원 예정 시기 |
| 스스로 쓰는 회고 | "이번 주에 배운 것 / 다음 주에 고칠 것" 식의 짧은 회고 |

과제 7의 "위험 구역" UI 패턴을 재사용해 **경계를 시각적으로도 명확히** 긋는다 — `<hr>` +
"🔒 나만 보는 자리" 라벨 + 옅은 배경색 박스(T08-C13).

### 결정적 설계 — 왜 view-source에 안 남는가

비공개 섹션의 마크업은 처음부터 **빈 컨테이너**로만 존재한다. 페이지 로드 시
`fetch('/api/me')`로 세션을 확인하고, 200이 와야만 `fetch('/api/private-notes')`로 실제
내용을 받아 그 컨테이너에 주입한다. 로그인하지 않은 상태로 받은 최초 HTML 문서에는
비공개 문자열이 **애초에 존재하지 않는다** — CSS로 숨기는 방식과 원천적으로 다르다
(T08-C15·C18).

### 서버 쪽 거절

`/api/private-notes`는 요청 쿠키의 세션이 없거나 만료됐으면 **401**(로그인 안 함) 또는
**404**(있는지 없는지도 알려주지 않음 — 아래 카드 5) 로 즉시 끊고 몸통을 비워 돌려준다.
브라우저 주소창에 직접 쳐도, `curl`로 쳐도 결과는 같다(T08-C16·C17).

| 통과 기준 | 대응 |
|-----------|------|
| T08-C13~C14 | `index.html`의 `#private` 섹션 + 위 3항목 |
| T08-C15·C18 | 클라이언트 사이드 주입 방식(빈 컨테이너 → 인증 후 채움) |
| T08-C16·C17 | `/api/private-notes` 세션 검사 → 401 |

---

## 2. 패스키를 등록한다 (카드 2)

### 고른 것 — WebAuthn(FIDO2) + `@simplewebauthn/server` 13.3.3

브라우저 내장 `navigator.credentials.create()`가 W3C WebAuthn 표준을 구현한다. 서버 쪽
챌린지 생성·attestation 검증은 `@simplewebauthn/server`(SimpleWebAuthn, MIT 라이선스,
Vercel/Auth0 등에서 널리 쓰임)에 맡긴다 — CBOR attestation object를 손으로 파싱하지 않는다.

> **만들면서 바꾼 것** — 처음에는 브라우저 쪽도 `@simplewebauthn/browser`를 CDN에서
> 불러 쓰려 했다. 실제로 붙여 보니 브라우저 쪽이 하는 일은 **base64url ↔ ArrayBuffer 변환
> 40줄이 전부**였다. 그 40줄 때문에 페이지가 외부 CDN에 의존하게 되는 편이 손해라
> 판단해 `app.js`에 직접 썼다. 대신 **서명·origin·challenge를 판단하는 쪽**(실수가 곧
> 구멍이 되는 자리)은 그대로 라이브러리에 맡겼다. 설명서 ①에 이 구분을 그대로 적는다.

### 검토했지만 고르지 않은 것

| 후보 | 고르지 않은 이유 |
|------|------------------|
| CBOR·COSE 파싱을 직접 구현 | WebAuthn 규격 자체가 이미 재생 공격·origin 바인딩·서명 검증을 다 정의해뒀다. 이번 과제의 배점은 "표준 프로토콜을 흐름까지 이해하고 잘 골랐는지"이지 바이트 파서를 재발명했는지가 아니다(과제 7 카드 1과 같은 원칙) |
| Firebase Auth / Auth0 Passkeys(SaaS 위임) | 챌린지 생성·검증이 전부 블랙박스가 되어 카드 2·3이 요구하는 "서버가 챌린지를 어떻게 만들고 확인하는지"를 설명할 수 없다 |
| 아이디+비밀번호 폴백 남겨두기 | 원칙 1 위반 — 폴백이 있으면 결국 비밀번호를 "안 쓴 척"만 하는 것이다 |

### 등록 흐름

1. `POST /api/register/options` — 로그인 안 된 상태면 **계정 id만 미리 뽑아 두고(아직 만들지
   않는다)**, `generateRegistrationOptions()`로 챌린지를 만들어 `pk_challenges`
   (type='registration', `expires_at` = 2분 뒤)에 저장한 뒤 옵션을 응답으로 돌려준다.
   로그인된 상태로 호출되면(카드 4의 "두 번째 패스키 추가") 같은 계정에
   `excludeCredentials`로 기존 자격증명을 제외해 중복 등록을 막는다.

   > 계정을 여기서 만들지 않는 것이 T08-C25의 핵심이다. 미리 만들어 두면 사용자가 지문 창을
   > 닫았을 때 빈 계정이 남아 "아무것도 저장되지 않는다"가 거짓이 된다.
2. 브라우저가 `navigator.credentials.create(options)` 호출 → 플랫폼 authenticator(윈도우
   Hello/터치 ID/보안 키/브라우저의 패스키 관리자)가 키 쌍을 만들고 **공개키만** 담은
   attestation을 돌려준다. 개인키는 authenticator 내부를 벗어나지 않는다.
3. `POST /api/register/verify` — 챌린지가 아직 안 쓰였고 만료 전인지 확인 →
   `verifyRegistrationResponse()`로 서명·origin·rpID 검증 → 통과하면 `pk_credentials`에
   `id`(credential id)·`public_key`·`counter`(0)·`device_name`(사용자가 붙인 이름)을 저장,
   `pk_challenges.used_at`을 지금 시각으로 찍는다 → 세션 쿠키 발급.

### 통과 기준과의 대응

| 통과 기준 | 대응 |
|-----------|------|
| T08-C19·C20 | `generateRegistrationOptions()`가 요청마다 `crypto.randomBytes` 기반 challenge 생성 — 등록 요청 두 번을 나란히 캡처해 값이 다름을 보인다 |
| T08-C21·C22 | 검증 통과 직후 `pk_credentials` 행 캡처 + "이 값은 공개키이지 비밀번호가 아니다" 설명 |
| T08-C23 | 등록 요청 본문(`attestationObject`)에 개인키 관련 필드가 없다는 것을 실제 페이로드로 확인 — WebAuthn 스펙상 애초에 그런 필드가 존재하지 않는다는 점도 함께 기록 |
| T08-C24 | 등록 폼에 기기 별명 입력란(`device_name`) |
| T08-C25 | 사용자가 authenticator 프롬프트에서 취소(`NotAllowedError`) → 화면에 "등록을 취소했습니다" 안내, `pk_credentials`에 아무 행도 안 생김을 조회로 확인 |
| T08-C26 | 실제 등록 시 브라우저가 어디에 저장했는지(예: 크롬 프로필의 구글 비밀번호 관리자, 또는 실제 보안 키)를 진짜로 등록해보고 기록 |

---

## 3. 패스키로 들어간다 (카드 3)

### 로그인 흐름 — usernameless(discoverable credential)

아이디를 입력하지 않는다. `POST /api/login/options`가 `allowCredentials`를 비운 채
`generateAuthenticationOptions()`로 챌린지만 발급하면, `navigator.credentials.get()`이
**브라우저가 이 사이트에 등록해 둔 패스키 중에서 사용자가 고르게** 한다(discoverable
credential). 고른 자격증명의 `id`로 서버가 `pk_credentials`를 찾아 어느 계정인지 알아낸다.

1. `POST /api/login/options` — 새 챌린지 생성(type='authentication') → `pk_challenges` 저장.
2. `navigator.credentials.get()` → authenticator가 저장해 둔 개인키로 챌린지에 **서명**해
   돌려준다.
3. `POST /api/login/verify` — 챌린지 미사용·미만료 확인 → 응답에 실린 `credential.id`로
   `pk_credentials` 조회 → 저장된 공개키로 `verifyAuthenticationResponse()` 서명 검증 →
   `counter`가 이전보다 커졌는지 확인(복제된 authenticator 탐지) → 통과하면 `pk_sessions`에
   새 행(랜덤 uuid, 만료 12시간 뒤)을 만들고 그 id를 `HttpOnly; Secure; SameSite=Lax` 쿠키로
   내려준다. 챌린지는 `used_at`을 찍어 소진한다.

### 무엇으로 사람을 기억하는가 — 세션(토큰 아님)

발급하는 것은 **불투명한 세션 id**일 뿐, 그 안에 아무 정보도 담기지 않는다(JWT처럼 서명된
클레임이 아니다). `/api/me`가 매 요청마다 `pk_sessions` 테이블을 직접 조회해 유효성을
확인한다 — 로그아웃(`/api/logout`)은 그 행을 지우는 것이고, 지우는 즉시 **그 세션 id로는
아무것도 조회되지 않는다**(과제 7의 `session_is_active()`가 사후 땜질이었다면, 이번엔 설계
자체가 stateful이라 같은 문제가 애초에 생기지 않는다).

### 확인 순서 (성공 요청과 거절 요청을 나란히)

1. 로그인 상태로 `/api/private-notes` 호출 → 200 + 내 자료.
2. 같은 챌린지 응답을 **다시** `/api/login/verify`에 보냄 → `used_at`이 이미 찍혀 있어 거절
   (T08-C31, 재생 공격 방어).
3. 로그아웃 → 저장해 둔 세션 쿠키 값으로 `/api/private-notes` 재요청 → 401(T08-C33).

| 통과 기준 | 대응 |
|-----------|------|
| T08-C27·C28 | 로그인 요청 두 번의 challenge 값을 나란히 캡처 |
| T08-C29·C30 | 성공 검증 로그 + (공개키 안 맞는) 실패 케이스를 나란히 |
| T08-C31 | 위 확인 순서 2번 |
| T08-C32 | "세션 — `pk_sessions` 테이블에 서버가 직접 들고 있다"를 설명서에 명시 |
| T08-C33 | 위 확인 순서 3번 |
| T08-C34 | 캡처에서 세션 쿠키 값은 `••••` 처리 |
| T08-C35 | 소스 전체에 `type="password"` input이 없음을 grep으로 확인 |

---

## 4. 기기를 잃어버렸을 때 (카드 4)

### 두 번째 패스키 등록

로그인한 상태에서 "패스키 추가" 버튼 → `/api/register/options`가 이번엔 새 계정을 만들지
않고 **현재 세션의 계정**에 등록 챌린지를 발급하며, `excludeCredentials`에 이미 등록된
자격증명 id들을 넣어 같은 authenticator로 중복 등록하는 것만 막는다(다른 authenticator라면
등록 허용). 두 번째 authenticator(다른 기기 또는 다른 브라우저 프로필)로 이 흐름을 한 번 더
탄다.

### 목록 화면과 삭제

`/api/me`가 `pk_credentials` 중 현재 계정 소유분을
`{id, 앞 12자, device_name, created_at, public_key, counter}` 형태로 돌려주고, 화면에
목록으로 그린다(T08-C43). 각 행에 삭제 버튼 → `DELETE /api/credentials/:id` — 세션의 계정
소유가 맞는지 서버가 확인 후 삭제.

> **공개키를 응답에 실어 화면에서도 펼쳐 보인다.** 공개키는 이름 그대로 공개해도 되는
> 값이고(그 값으로는 서명을 확인만 할 수 있지 만들 수 없다), 이렇게 해 두면 카드 2의
> "서버에 저장된 값이 공개키이지 비밀번호가 아니다"(T08-C22)를 **DB를 열어 보지 않고
> 화면만으로** 보일 수 있다. 로컬이든 배포든 같은 방법으로 확인된다.

삭제 확인은 브라우저 기본 `confirm()`이 아니라 **화면 안에 펼쳐지는 인라인 경고**로 만든다.
기본 대화상자는 촬영이 안 되고, T08-C46은 경고가 **화면에** 있기를 요구하기 때문이다.

### 마지막 패스키를 지울 때 — 일부러 막지 않는다

패스키가 1개 남았을 때 지우면 **그 계정은 다시 못 들어간다**(이메일도 비밀번호도 없으니
복구 수단이 없다 — 이게 패스워드리스의 실제 비용이다). 이걸 기술적으로 막지 않고, 대신
확인창에 "⚠ 마지막 패스키입니다. 지우면 이 계정에 다시 들어올 방법이 없습니다"라고
명시적으로 경고한 뒤 사용자가 그래도 지우면 지운다. 통과 기준 T08-C46이 요구하는 것은
"막았다"가 아니라 **"어떻게 되는지 화면과 제출문에 적혀 있는가"**이므로, 이 상태를 실제로
한 번 만들어 캡처하고 설명서에 그대로 남긴다.

| 통과 기준 | 대응 |
|-----------|------|
| T08-C42 | 두 번째 authenticator로 두 번째 패스키 등록 |
| T08-C43 | `/api/me`의 자격증명 목록 화면 |
| T08-C44 | 하나 삭제 → 남은 것으로 로그인 성공 캡처 |
| T08-C45 | 삭제한 자격증명 id로 로그인 시도 → `pk_credentials`에서 조회 자체가 안 되어 거절되는 기록 |
| T08-C46 | 마지막 패스키 삭제 확인창 캡처 + 설명서에 "복구 수단 없음"을 그대로 명시 |

---

## 5. 정말 안 열리는지 확인하고, 어떻게 붙였는지 적기 (카드 5)

### 두 계정 사이 격리 확인

스크래치 계정 A·B를 **서로 다른 virtual authenticator**로 각각 등록해 만든다(아래 도구
절 참고). 각 계정에 서로 다른 비공개 메모를 하나씩 저장한다(T08-C36).

- `GET /api/private-notes/:id` — A로 로그인한 채 B의 메모 id를 직접 넣어 요청 → 404
  (있는지 없는지조차 알려주지 않는다). 반대 방향도 동일(T08-C37·C38).
- 거절 전후로 "내 목록에 남의 메모가 하나도 안 늘지 않았다"는 건수 대조(T08-C39).
- `POST /api/private-notes`의 본문에 `user_id: <B의 id>`를 억지로 적어 보내도, 서버가
  세션에서 뽑은 A의 id로만 저장 — 저장 직후 다시 읽어 실제 소유자가 A인지 확인
  (T08-C40, 과제 7의 `stamp_owner` 검증과 동일한 방식).
- 이 강제 로직이 있는 파일 위치(`api/private-notes.js`의 특정 줄)를 설명서에 적는다
  (T08-C41).

### 인증 구현 설명서 (여섯 항목)

① 무엇으로 붙였나 — WebAuthn(FIDO2), `@simplewebauthn/server`+`@simplewebauthn/browser`,
직접 구현하지 않고 표준 라이브러리를 씀(T08-C48).
② 검토했지만 고르지 않은 것 — 위 카드 2의 표.
③ 비밀번호를 어떻게 맡아 두나 — 애초에 비밀번호가 없다는 사실 자체가 답.
④ 들어온 사람을 어떻게 기억하나 — `pk_sessions` 세션(토큰 아님), 로그아웃=행 삭제.
⑤ 등록·로그인·응답·비공개 조회, 네 흐름이 소스 어디를 지나는지 다이어그램(카드 2·3의
    엔드포인트 표 재사용) + 확인 네 가지 나란히(T08-C49·C50):
    1. 로그인 없이 `/api/private-notes` 열기 → 401
    2. B 계정으로 A의 메모 id 요청 → 404
    3. 이미 쓴 challenge로 재로그인 시도 → 거절
    4. 마지막 패스키 삭제 뒤 로그인 시도 → 등록된 자격증명이 없어 실패
⑥ 아직 못 막은 것 — **만들고 나서 실제로 확인한 다섯 가지**(미리 정하지 않고, 구현을
    마친 뒤 검토해 적었다):

| 못 막은 것 | 왜 그런가 |
|-----------|-----------|
| **잠금 해제된 기기를 그대로 쓰는 사람** | `userVerification: "preferred"`로 두었다. PIN 없는 보안 키도 쓸 수 있게 하려는 선택인데, 그 대가로 기기가 이미 잠금 해제돼 있으면 지문 확인 없이 패스키가 응답할 수 있다. `"required"`면 막히지만 일부 보안 키를 못 쓴다 |
| **12시간 세션 동안 재확인이 없다** | 로그인 뒤에는 패스키 삭제 같은 민감한 동작에도 패스키를 다시 요구하지 않는다 |
| **비공개 자료가 서버에 평문으로 있다** | 자료 자체를 암호화하지 않았다. `SUPABASE_SERVICE_ROLE_KEY`가 새면 전부 읽힌다 — 패스키가 막는 건 바깥에서 들어오는 길이지 서버 안에서 보는 눈이 아니다 |
| **계정 복구 수단이 아예 없다** | 일부러 그렇게 뒀지만(원칙 1), 실제 서비스라면 복구 코드가 필요하다 |
| **다 쓴 challenge를 청소하지 않는다** | `pk_challenges`가 계속 쌓인다. 동작에는 문제없지만(매번 만료·사용 여부를 확인한다) 관리 작업이 빠져 있다 |

| 통과 기준 | 대응 |
|-----------|------|
| T08-C01·C02·C03 | 배포된 HTTPS 주소, 소스 저장소 주소, 새 시크릿창에서 계정 없이 열림 |
| T08-C10 | 결과물 첫 화면 = 공개 소개 페이지(아무것도 등록 안 해도 보임) |
| T08-C11·C12 | 카드 1의 "공개 영역 — 과제 1 그대로" + 제출물에 실 개인정보 없음(전부 지어낸 내용) |
| T08-C36~C41 | 위 두 계정 격리 확인 |
| T08-C47·C48 | 설명서 ①~⑥ 구조 + 라이브러리 이름 명시 |
| T08-C49·C50 | 위 ⑤ |
| T08-C51 | 위 ⑥ |
| T08-C52 | 짧은 확인 방법 4줄(아래 검증 안내서) |
| T08-C53 | AI 3줄 — 이 설계도/구현에서 실제로 내가 판단한 지점과 AI 제안을 따르지 않은 지점을 구현 후 정리 |

---

## DB 스키마

실제 스키마는 [portfolio-passkey/supabase/schema.sql](portfolio-passkey/supabase/schema.sql)에 있다.
초안에서 달라진 곳:

- **공개키는 `bytea`가 아니라 `text`(base64url)로 저장한다.** PostgREST로 바이너리를 주고받는 것보다
  문자열이 다루기 쉽고, 로컬 파일 저장소와도 같은 모양이 된다.
- **`pk_challenges.user_id`에 FK를 걸지 않는다.** 신규 등록의 challenge는 "아직 존재하지 않는 계정"을
  가리키기 때문이다. 계정은 확인 단계를 통과해야 비로소 만들어진다 — 그래야 T08-C25("등록을 중간에
  취소하면 서버에 아무것도 저장되지 않는다")가 실제로 참이 된다.
- `pk_users.display_name`, `pk_challenges.is_new_account/display_name/device_name`이 추가됐다.
  등록 폼에서 받은 패스키 이름을 확인 단계까지 들고 가야 해서다.

## API 엔드포인트 (`portfolio-passkey/api/*.js`, Vercel Node 서버리스 함수)

| 경로 | 메서드 | 인증 필요 | 하는 일 |
|------|--------|-----------|---------|
| `/api/register/options` | POST | 아니오(신규) / 세션(추가 등록) | 등록 챌린지 발급 |
| `/api/register/verify` | POST | 아니오 | attestation 검증, 자격증명 저장, 세션 발급 |
| `/api/login/options` | POST | 아니오 | 로그인 챌린지 발급(usernameless) |
| `/api/login/verify` | POST | 아니오 | assertion 검증, 세션 발급 |
| `/api/logout` | POST | 세션 | `pk_sessions` 행 삭제 |
| `/api/me` | GET | 세션 | 내 자격증명 목록 반환, 없으면 401 |
| `/api/private-notes` | GET/POST | 세션 | 목록 조회 / 생성(본문의 `user_id`는 무시) |
| `/api/private-notes/[id]` | GET | 세션 | 단건 조회, 남의 것이면 404 |
| `/api/credentials/[id]` | DELETE | 세션 | 내 자격증명 삭제(남의 것이면 404) |

서버 코드 전체에서 **DB 접근은 오직 `lib/db.js`(service_role 클라이언트) 하나를 통해서만**
이뤄지고, 그 파일을 import하는 곳은 `api/` 폴더뿐이다. `user_id`를 클라이언트 입력에서
가져오는 코드가 있으면 그 자체가 버그로 취급한다(원칙 4).

## 도구 (`tools/check.mjs`·`capture.mjs`·`report.mjs`)

과제 6·7에서 쓰던 CDP 직결 하네스를 그대로 재사용하되, WebAuthn 자동화를 위해
**CDP `WebAuthn` 도메인의 virtual authenticator**를 쓴다(실제 지문·보안키 없이도 등록·
로그인을 스크립트로 재현할 수 있다):

```
WebAuthn.enable
WebAuthn.addVirtualAuthenticator({
  options: { protocol: 'ctap2', transport: 'internal',
             hasResidentKey: true, hasUserVerification: true,
             isUserVerified: true, automaticPresenceSimulation: true }
})
```

**만들면서 알게 된 것 — 가상 기기는 한 번에 하나만 붙여야 한다.** 두 개를 동시에 붙여
두면 아이디 없는 로그인(discoverable) 때 어느 기기가 답했는지가 불분명해진다. 그래서
`tools/harness.mjs`의 `useDevice(이름)`은 **지금 붙은 기기의 자격증명을
`WebAuthn.getCredentials`로 통째로 저장해 두고 떼어 낸 뒤**, 새 기기를 붙이고 필요하면
`WebAuthn.addCredential`로 다시 심는다. 덕분에 이런 것들이 그대로 재현된다:

- 계정 A·B 격리(카드 5) → 서로 다른 기기로 각각 등록해 계정 두 개를 만든다.
- 기기 분실(카드 4) → 기기1로 등록 → 기기2로 패스키 추가 → 서버에서 기기1의 패스키 삭제 →
  **기기1을 다시 꽂아** 로그인 실패를 확인 → **기기2로 바꿔 꽂아** 로그인 성공을 확인.

`check.mjs`는 검사 35개를 판정하고, `capture.mjs`는 같은 하네스로 사진 10장 + 화면으로는
못 보이는 값(challenge·요청 본문 필드·응답 상태)을 글 기록 16건으로 남긴다. `report.mjs`는
그 둘을 읽어 "인증 구현 설명서" 여섯 항목 구조의 PDF로 묶는다 — **숫자와 응답 문구를 손으로
옮겨 적지 않는다.**

## 검증 안내서 (T08-C52, 4줄)

- **어디로 가나요** — 공개 주소(소개 페이지부터 시작)
- **세 단계 안에 무엇을 하나요** — ① 패스키 등록 ② 비공개 자리에 메모 하나 남기기 ③
  로그아웃 뒤 같은 자리 주소를 다시 열기
- **무엇이 보이면 통과인가요** — ①,② 뒤엔 비공개 자리가 보이고, ③ 뒤엔 다시 안 보인다
- **안 될 때는** — 브라우저가 패스키를 지원하지 않으면 안내 문구가 뜬다 / 등록 중 취소하면
  아무 것도 저장되지 않는다

---

## 순서 / 블로커

**끝난 것 (로컬에서 검사 35개 전부 통과):**
- `portfolio-passkey/` 폴더 — `cb23773`에서 `index.html`·`po.md`를 꺼내 그 위에 이어붙였다.
- 비공개 섹션, `app.js`의 WebAuthn 클라이언트, `api/` 전체, `lib/` 전체, `supabase/schema.sql`.
- `tools/harness.mjs`·`check.mjs`(35개)·`capture.mjs`(사진 10 + 기록 16)·`report.mjs`(PDF).
- 로컬 개발은 `vercel dev` 대신 `tools/dev-server.mjs`를 썼다. Vercel CLI를 깔지 않아도 되고,
  **핸들러 파일은 로컬과 배포가 완전히 같은 코드**라 로컬 통과가 배포 통과와 같은 뜻이 된다.
  Supabase 환경변수가 없으면 `.dev-store/store.json` 파일 하나를 저장소로 쓴다.

**남은 하드 블로커 (사용자 액션 필요):**
1. 새 Supabase 프로젝트 생성 + `supabase/schema.sql` 실행 + `SUPABASE_URL`·
   **`SUPABASE_SERVICE_ROLE_KEY`**. 이 키는 anon 키보다 훨씬 민감하다 — 채팅에 붙여넣기보다
   Vercel 환경변수 UI에 직접 입력하는 쪽을 권한다.
2. 새 Vercel 프로젝트(`aleph-passkey`) 생성, Root Directory를 `portfolio-passkey`로,
   Framework Preset은 `Other`(빌드 없음). 환경변수는 Production+Preview 둘 다.
3. 배포 뒤 `RP_ORIGINS`는 따로 넣지 않아도 된다 — Vercel이 주는
   `VERCEL_PROJECT_PRODUCTION_URL`을 `lib/rp.js`가 자동으로 허용 목록에 넣는다.

**그다음:** 공개 주소에서 `BOARD_URL=... node tools/check.mjs --json` → `capture.mjs` →
`report.mjs`로 최종 증빙을 다시 만든다. **실제 기기(지문/Face ID)로 한 번 직접 등록해
어디에 저장됐는지 확인하는 것(T08-C26)은 자동화로 대신할 수 없다** — 배포 뒤 직접 해야 한다.

## 미정 사항

- 세션 만료 시간(현재 12시간) — 실사용해 보고 조정.
- 두 번째 패스키를 무엇으로 둘지 — 보안 키가 있으면 그것, 없으면 다른 기기(휴대폰).
