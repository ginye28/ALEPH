# 플랜두씨 다이어리 1 — 계획한 나와 실제의 나 · 설계도

내가 실제로 남기고 싶은 항목을 **기록·수정·삭제하고 다시 불러오는** 개인 기록기.
입력 화면을 만드는 데서 끝나지 않고, **구조가 바뀌어도 이전 기록을 지키는 법**과
**잘못된 값이 집계를 흔들지 않게 막는 법**을 익힌다.

폴더는 `plandosee/`. 과제 4 `today-dashboard`와 과제 5 `handoff-lab` 옆에 나란히 두고,
그 둘은 제출 증거라 건드리지 않는다. 공개 주소는 Vercel 새 프로젝트로 따로 판다.

---

## 0. 이 설계도의 기준

과제 카드 5개는 아래 **다섯 가지 원칙**에서 따라 나온다. 구현 중 판단이 갈리면 이 다섯 줄로 되돌아온다.

| # | 원칙 | 이 원칙이 막아주는 실패 |
|---|------|------------------------|
| 1 | **공개 화면의 데이터는 전부 합성이다.** 실제 기록은 내 PC에만 두고, 공개 주소에는 합성 기록과 가림 처리한 요약만 올린다. | 완료 기준의 "실제 개인 기록과 개인정보가 보이지 않는다"를 어기는 문제. 한 번 올라가면 되돌릴 수 없다 |
| 2 | **행을 가리키는 것은 배열 순번이 아니라 고유 ID다.** 수정·삭제·중복 판정 모두 `id`로만 한다. | 정렬이나 필터를 바꾼 순간 수정·삭제가 **다른 행에 반영**되는 문제 (카드 1의 대표 함정) |
| 3 | **저장 전에 검사하고, 집계 전에 한 번 더 거른다.** 잘못된 값은 버리지 않고 `보류` 칸에 이유와 함께 남긴다. | 잘못된 날짜나 값이 **조용히 집계에 섞이는** 문제. 조용히 버려도 같은 죄다 (카드 4의 핵심) |
| 4 | **변환은 몇 번을 돌려도 결과가 같다.** `schemaVersion`을 보고 이미 변환된 기록은 건너뛴다. | 두 번째 변환에서 기록이 늘거나 기본값이 사용자 입력을 덮어쓰는 문제 (카드 3의 핵심) |
| 5 | **검사는 사람 눈이 아니라 `node tools/check.mjs` 한 줄이 판정한다.** | "돌려봤는데 되던데요"가 근거가 되는 문제. 과제 5에서 검사기 자체의 결함까지 겪었으므로 **FAIL이 나오면 두 번 돌려본 뒤에** 코드를 고친다 |

> **경과 기간 원칙** — 별도 경과 기간 **5일**은 작업시간에 넣지 않는다. 기다리는 동안 카드 2~4의
> 검사를 끝낸다. 날짜를 앞당기거나 지어내지 않는다.

---

## 1. 무엇을 기록하는가 (카드 1)

**기록 문장** — 카드 1이 요구하는 `무엇을 / 어떤 숫자나 상태로 / 어느 날짜와 시간대에`:

> **공부한 시간을 분 단위로, Asia/Seoul 기준 날짜에 기록한다.**

### 왜 이 항목인가

| 후보 | 문제 |
|------|------|
| 체중 | 하루 한 번이라 주간 집계가 5건뿐. 카드 4의 중복·경계 시험이 빈약해진다 |
| 운동 여부(O/X) | 값이 불리언이라 **합계·평균이 의미 없다**. 카드 5의 "요약값이 함께 바뀐다"를 보이기 어렵다 |
| **공부 시간(분)** | 하루 여러 건, 더하면 의미가 생김, 잘못된 값(음수·문자·과다)을 자연스럽게 만들 수 있음 |

숫자이면서 **하루에 여러 건이 쌓이는** 항목이라야 주간 합계가 뜻을 갖고, 잘못된 값 시험도 억지스럽지 않다.

### 기록 한 건의 필드

| 필드 | 형 | 규칙 | 왜 필요한가 |
|------|-----|------|-------------|
| `id` | string | `crypto.randomUUID()` | 원칙 2. 순번으로 가리키면 정렬만 바꿔도 엉뚱한 행이 지워진다 |
| `date` | string | `YYYY-MM-DD`, Asia/Seoul 기준 | 집계의 단위. 기기 시간대에 의존하지 않는다 |
| `timezone` | string | 항상 `"Asia/Seoul"` | 화면에 기준 시간대를 표시해야 한다(카드 1 통과 기준) |
| `subject` | string | 1~40자, 필수 | 무엇을 공부했는지 |
| `minutes` | number | 정수 1~1440 | 값. 단위는 분으로 고정 |
| `unit` | string | 항상 `"분"` | 화면에 단위를 표시해야 한다 |
| `memo` | string | 0~200자, 선택 | |
| `tag` | string | **v2에서 추가** (카드 3) | |
| `createdAt` | string | ISO | 같은 날 여러 건의 순서 |

`minutes`의 상한을 1440(하루 분)으로 두는 이유 — 오타로 `600` 대신 `6000`을 넣었을 때
주간 합계가 통째로 망가진다. **입력 시점에 막는 게 집계에서 걸러내는 것보다 싸다.**

### 생성·조회·수정·삭제

- **생성** — 필수값(`date`·`subject`·`minutes`)이 비면 저장하지 않고 각 칸 아래에 이유를 쓴다
- **조회** — 날짜 내림차순, 같은 날은 `createdAt` 오름차순
- **수정** — 행의 `수정`을 누르면 그 자리에서 편집. 저장하면 **그 `id` 한 건만** 바뀐다
- **삭제** — 확인 한 번 뒤 그 `id` 한 건만 사라진다

---

## 2. 저장·내보내기·전체 삭제 (카드 2)

### 저장 위치는 한 곳

`localStorage`의 `plandosee.records.v2` **하나만** 쓴다. 읽는 곳과 쓰는 곳이 갈라지면
"새로고침하면 사라진다"와 "전체 삭제 뒤 일부가 남는다"가 동시에 생긴다.

```
{ "schemaVersion": 2, "records": [ … ] }
```

### 내보내기 — JSON

CSV가 아니라 JSON을 고른다. **CSV는 메모의 쉼표·줄바꿈에서 형식이 깨지고**, 그걸 막으려면
따옴표 규칙을 직접 구현해야 한다. 이 과제의 점수는 그 구현에서 나오지 않는다.

파일 이름은 `plandosee-내보내기-YYYY-MM-DD.json`.

### 가져오기 — 세 가지를 구분해 보여준다

| 입력 | 결과 | 화면 문구 |
|------|------|-----------|
| 정상 JSON | 복원 | `n건을 불러왔습니다` |
| JSON은 맞지만 형식이 다름 | **기존 기록 유지** | `records 배열이 없습니다` |
| JSON이 아님 (깨진 파일) | **기존 기록 유지** | `파일을 JSON으로 읽지 못했습니다 (위치 n)` |

**손상 파일이 기존 기록을 지우지 않는다**가 카드 2의 핵심이다. 그래서 순서를 지킨다 —
**읽기 → 검사 → 통과한 경우에만 쓰기.** 지우고 나서 읽지 않는다.

중복 방지는 `id` 기준(원칙 2). 같은 `id`가 이미 있으면 건너뛰고 `n건은 이미 있어 건너뜀`으로 알린다.

### 전체 삭제

`localStorage.removeItem` 한 번 + 화면 상태를 저장소에서 **다시 읽어** 그린다.
메모리에서만 비우면 새로고침 때 되살아난다.

---

## 3. 형식 변경과 자동 변환 (카드 3)

### v1 → v2

새 필드는 **`tag`** 하나. v1에는 없던 칸이다.

```
v1: { id, date, timezone, subject, minutes, unit, memo, createdAt }
v2: { …v1, tag: "" , schemaVersion: 2 }
```

### 변환 규칙

| 상황 | 처리 |
|------|------|
| `schemaVersion`이 없음 | v1으로 보고 변환. `tag`에 기본값 `""` |
| `schemaVersion === 2` | **건너뛴다** (원칙 4) |
| 이미 `tag`가 있는 v1 기록 | 기존 값을 **덮어쓰지 않는다** |

두 번 돌려도 결과가 같아야 한다. 검사에서 **변환 → 변환**을 연달아 실행하고
건수·합계·`id` 목록이 모두 같은지 대조한다.

### 화면에 보여야 하는 것

카드 3의 통과 기준이 "**현재 자료 형식과 변환 상태가 화면에 보이고**"다. 그래서 목록 위에 한 줄:

```
자료 형식 v2 · 이전 형식 3건을 v2로 변환했습니다 (2026-08-30 10:12)
```

변환이 없었으면 `자료 형식 v2 · 변환할 이전 형식 기록이 없습니다`.

---

## 4. 주간 요약과 잘못된 값 (카드 4)

### 주의 경계

**월요일 00:00 ~ 일요일 23:59, Asia/Seoul.** 화면에 기간을 그대로 쓴다.

```
2026-08-24(월) ~ 2026-08-30(일) · 합계 340분 · 유효 12건 · 보류 3건
```

날짜 문자열은 `Date` 객체로 바꾸지 않고 **`YYYY-MM-DD` 문자열 비교**로 주를 판정한다.
`new Date("2026-08-24")`는 UTC 자정으로 해석돼 KST에서 하루 밀린다. 과제 4에서 같은 함정을 겪었다.

### 예상 결과표 — 먼저 쓰고 나서 구현한다

| 입력 | 처리 | 이유 |
|------|------|------|
| `2026-08-24` 00:00 기록 | **집계 포함** | 주 시작 경계 |
| `2026-08-30` 23:59 기록 | **집계 포함** | 주 끝 경계 |
| `2026-08-31` 기록 | 다음 주 | |
| `minutes: null` | **보류** — `값이 비었습니다` | |
| `minutes: "삼십"` | **보류** — `숫자가 아닙니다` | |
| `minutes: -10` | **보류** — `1 이상이어야 합니다` | |
| `minutes: 5000` | **보류** — `1440 이하여야 합니다` | |
| `date: "2026-13-45"` | **보류** — `날짜 형식이 아닙니다` | |
| `date: "2026-02-30"` | **보류** — `없는 날짜입니다` | |
| 같은 `id` 두 건 | 뒤엣것 **건너뜀** — `id 중복` | 원칙 2 |

### 보류는 숨기지 않는다

잘못된 값을 조용히 버리면 사용자는 자기 기록이 사라진 줄 안다. 목록 아래에 **보류 칸**을 두고
행마다 이유를 쓴다. **보류 행은 집계에 들어가지 않는다** — 이게 카드 4의 통과 기준이다.

---

## 5. 5일 사용과 개선 (카드 5)

### 실제 기록은 PC에

원칙 1대로 실제 5일치는 로컬에서만 쓴다. 공개 화면에 올리는 것은 **가림 처리한 요약**뿐이다.

| 날짜 | 건수 | 합계(분) |
|------|------|----------|
| 1일차 | 3 | 95 |
| … | | |

과목명·메모는 올리지 않는다. 날짜도 `1일차`처럼 상대 표기로 바꾼다.

### 개선은 한 가지만

5일 쓰면서 **반복해서** 불편했던 것 하나로 좁힌다. 지금 예상되는 후보:

1. 같은 과목을 매번 다시 타이핑 — 최근 과목 버튼
2. 어제 것을 빠뜨림 — 날짜 기본값을 오늘로
3. 분 입력이 번거로움 — `+10` `+30` 버튼

**실제로 5일 써 보기 전에는 고르지 않는다.** 예상과 실제는 다르고, 카드 5는 "실제로 불편했던 점"을 요구한다.

### 통과 기준을 화면으로

"가상 기록을 고치면 **해당 행과 관련 요약값이 같은 화면에서 함께 바뀐다**" — 그래서
목록과 주간 요약을 **한 화면에** 둔다. 스크롤해야 요약이 보이면 채점자가 둘이 같이 바뀌는 걸 못 본다.

---

## 6. 구조

```
plandosee/
├─ src/
│  ├─ core/
│  │  ├─ validate.js      필드 검사 → { ok, reason }
│  │  ├─ migrate.js       v1 → v2, 멱등
│  │  ├─ weekly.js        주 경계 판정과 집계
│  │  └─ ids.js           고유 ID 발급
│  ├─ storage/
│  │  └─ records.js       localStorage 한 곳. 읽기 → 검사 → 쓰기
│  ├─ components/
│  │  ├─ RecordForm/      추가·수정 입력
│  │  ├─ RecordList/      목록 · 행별 수정/삭제
│  │  ├─ HeldList/        보류 행과 이유
│  │  ├─ WeeklySummary/   기간 · 합계 · 건수
│  │  └─ DataTools/       내보내기 · 가져오기 · 전체 삭제 · 오류 영역
│  ├─ fixtures/
│  │  ├─ synthetic-v1.json     카드 3 변환 시험용 (5건)
│  │  └─ edge-cases.json       카드 4 경계·오류 자료
│  └─ pages/Diary/
└─ tools/
   ├─ check.mjs           검사 실행기 (원칙 5)
   ├─ capture.mjs         증빙 촬영
   └─ report.mjs          보고서 PDF
```

과제 4·5에서 만든 `check.mjs`·`capture.mjs`·`report.mjs`의 **CDP 제어 뼈대를 그대로 가져온다.**
새로 배우는 비용을 쓰지 않는다.

---

## 7. 화면 구성

한 화면에 위에서부터:

1. **제목 + 기준 시간대** — `공부 기록 · 분 단위 · Asia/Seoul (UTC+9)`
2. **자료 형식 줄** — `자료 형식 v2 · 이전 형식 n건 변환함` (카드 3)
3. **입력 폼** — 날짜·과목·분·태그·메모, 칸 아래 오류 이유
4. **주간 요약** — 기간·합계·유효 건수·보류 건수 (카드 4·5)
5. **기록 목록** — 행마다 수정·삭제 (카드 1)
6. **보류 목록** — 행마다 이유 (카드 4)
7. **자료 도구** — 내보내기·가져오기·전체 삭제 + 결과/오류 영역 (카드 2)
8. **합성 자료 넣기** — 시험용 버튼 두 개 (v1 5건 / 경계·오류 자료)

8번이 있어야 채점자가 **빈 화면에서 시작하지 않는다.** 과제 4에서 배운 것 —
채점자가 직접 데이터를 만들어야 하면 15초 안에 판정할 수 없다.

---

## 8. 검사 10개 (원칙 5)

| # | 종류 | 검사 |
|---|------|------|
| 1 | 정상 | 필수값을 채워 저장하면 목록에 1건 늘고 주간 합계가 그만큼 늘어난다 |
| 2 | 정상 | 행의 수정으로 분을 바꾸면 **그 행만** 바뀌고 주간 합계가 같이 바뀐다 |
| 3 | 정상 | 행을 삭제하면 그 행만 사라지고 합계가 줄어든다 |
| 4 | 정상 | 새로고침 뒤에도 건수·`id`·값이 같다 |
| 5 | 오류 | 필수값이 비면 저장되지 않고 칸마다 이유가 보인다 |
| 6 | 오류 | 깨진 파일을 가져오면 **기존 기록이 그대로**이고 오류 이유가 보인다 |
| 7 | 오류 | `minutes: 5000`·`date: 2026-02-30`은 보류로 가고 집계에 섞이지 않는다 |
| 8 | 회귀 | v1 자료를 넣으면 v2로 변환되고 **두 번 변환해도** 건수·합계가 같다 |
| 9 | 회귀 | 월요일 00:00과 일요일 23:59 기록이 같은 주에 들어간다 |
| 10 | 회귀 | 전체 삭제 뒤 0건이고 새로고침해도 0건이다 |

---

## 9. 카드별 구현 순서

| 카드 | 만드는 것 | 통과를 보이는 검사 |
|------|-----------|-------------------|
| 1 | 필드 정의 · CRUD · 단위/시간대 표시 | 1 · 2 · 3 · 5 |
| 2 | 저장 · 내보내기 · 가져오기 · 전체 삭제 | 4 · 6 · 10 |
| 3 | `schemaVersion` · v1→v2 변환 · 형식 줄 | 8 |
| 4 | 검사·보류 · 주간 요약 | 7 · 9 |
| 5 | 5일 사용 → 개선 1가지 | 2 (행+요약 동시 변경) |

카드 4의 자료는 **카드 1을 끝낸 직후에 미리 만든다.** 5일을 기다리는 동안 할 일이 없으면
날짜를 지어내고 싶어진다.

---

## 10. 검증 안내서 골격

```
어디로 가나요: (공개 주소)

무엇을 하나요 (3단계)
1) 합성 자료 넣기 → 경계·오류 자료를 누른다
2) 목록에서 아무 행의 수정을 눌러 분을 바꾸고 저장한다
3) 자료 도구에서 내보내기를 누른 뒤 그 파일로 가져오기를 한다

무엇이 보이면 통과인가요
1) 주간 요약에 기간(월~일)·합계·유효 건수·보류 건수가 보이고, 잘못된 날짜·값은 보류 칸에 이유와 함께 있다
2) 고친 행 하나만 바뀌고 주간 합계가 같은 화면에서 함께 바뀐다
3) 건수가 늘지 않고 "n건은 이미 있어 건너뜀"이 보인다

안 될 때
- 목록이 비면 합성 자료 넣기를 먼저 누른다
- 가져오기가 실패하면 오류 영역의 이유를 읽는다. 기존 기록은 그대로다
- 전체 삭제 뒤에는 새로고침해도 0건이다
```

---

## 11. 미리 대비할 위험

| 위험 | 왜 생기나 | 막는 법 |
|------|-----------|---------|
| 수정·삭제가 다른 행에 반영 | 배열 순번으로 행을 가리킴 | 원칙 2 — `id`로만 가리킨다. 검사 2·3이 이걸 본다 |
| 날짜가 하루 밀림 | `new Date("YYYY-MM-DD")`가 UTC로 해석 | 문자열 비교로 주를 판정. 검사 9가 이걸 본다 |
| 손상 파일이 기록을 지움 | 지우고 나서 읽음 | 읽기 → 검사 → 쓰기 순서. 검사 6이 이걸 본다 |
| 두 번째 변환에서 중복 | 버전 확인 없이 변환 | `schemaVersion` 확인. 검사 8이 이걸 본다 |
| 실제 개인 기록이 공개됨 | 편하다고 배포본에 실제 기록 입력 | 원칙 1 — 공개 화면은 합성만. 제출 전 저장소·화면 전수 검색 |
| 5일을 못 채움 | 하루를 빠뜨림 | 빠진 날은 빠진 대로 두고 실제 날짜 5개까지 계속 기록. **날짜를 지어내지 않는다** |
| 검사기 자체의 결함 | 고정 대기가 실제 응답보다 짧음 | 과제 5에서 겪은 것 — FAIL이 나오면 **두 번 돌려본 뒤** 코드를 고친다 |

---

## 12. 지금 정해야 할 것

구현 전에 사람이 정해야 하는 항목이다. 여기서 갈리면 아래가 전부 흔들린다.

1. **기록 항목** — 위 제안은 `공부 시간(분)`. 다른 걸로 바꿔도 되지만 *하루 여러 건 · 숫자 · 더해서 의미 있음* 세 조건은 지켜야 카드 4·5가 성립한다.
2. **폴더 이름** — `plandosee/`로 제안. 기존 `card-matching-game`·`today-dashboard`·`handoff-lab`과 같은 규칙.
3. **공개 주소** — Vercel 새 프로젝트, Root Directory를 `plandosee`로. 과제 4·5 주소는 그대로 둔다.
4. **5일의 시작일** — 카드 1을 끝낸 날이 1일차다. 오늘 시작하면 5일차는 그날 + 4일.
