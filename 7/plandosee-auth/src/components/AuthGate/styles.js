import { css } from "@emotion/react";

export const loading = css`
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 40vh;
    color: var(--ink-faint);
    font-size: 14px;
`;
