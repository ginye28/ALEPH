# 플랜두씨 다이어리 2 — 인증을 붙이고, 어떻게 붙였는지 설명하기 · 설계도

과제 6(`plandosee/`)에 **가입·로그인·로그아웃**을 붙여 내 기록을 나만 보게 만들고,
"막았습니다"가 아니라 **막히는 장면(요청과 응답)**으로 증명한다.

> **이 과제는 새 프로젝트다.** 과제 번호가 바뀌면 결과물 주소·소스 폴더·백엔드 프로젝트를
> **전부 새로 판다.** 과제 6의 `plandosee/` 폴더·Vercel 프로젝트(`aleph-pds`)·Supabase
> 프로젝트는 **제출 증거라 건드리지 않는다.** 대신 그 폴더를 통째로 **복제**해 새 폴더에서
> 이어간다 — 복제 시점의 커밋이 새 폴더의 git 이력에 조상으로 남아야
> T07-C78("T07 소스 이력에 제출 당시의 고정 T06 commit이 조상으로 포함되어 있다")을
> 만족한다. 같은 저장소(`ALEPH`) 안에서 새 폴더로 복사해 커밋하면 자동으로 만족된다 —
> 별도로 브랜치를 따거나 이력을 새로 쓰지 않는다.

---

## 0. 이 설계도의 기준

과제 카드 5개는 아래 **여섯 가지 원칙**에서 따라 나온다. 구현 중 판단이 갈리면 이 표로 되돌아온다.

| # | 원칙 | 이 원칙이 막아주는 실패 |
|---|------|------------------------|
| 1 | **"막았다"는 문장이 아니라 막히는 장면을 남긴다.** 모든 확인은 성공 요청과 거절 요청을 **나란히** 적는다. | 채점 기준이 정확히 이 문장이다("근거는 막히는 장면") — 문장으로만 적으면 그대로 반려된다 |
| 2 | **비밀번호는 절대 손으로 다루지 않는다.** 저장·비교·재설정 전부 인증 서비스(Supabase Auth/GoTrue)에 맡긴다. | 소금값 누락·평문 로깅·비교 로직 실수 같은 "직접 구현"의 흔한 사고를 아예 없앤다 |
| 3 | **화면에서 숨기는 것과 서버가 거절하는 것은 다르다.** 모든 접근 통제는 **RLS(Row Level Security)**로, 화면 조건문이 아니라 DB 정책으로 건다. | 카드 4의 핵심 함정 — "목록에서만 안 보이게 했더니 URL을 직접 치면 열리더라" |
| 4 | **행마다 주인은 클라이언트가 아니라 서버가 정한다.** `user_id`는 트리거가 `auth.uid()`로 못박고, 클라이언트가 보낸 값은 무시한다. | 요청 본문에 남의 계정 ID를 적어 보내는 공격(T07-C123)을 구조적으로 막는다 |
| 5 | **과제 6의 append-only·소프트삭제 원칙은 그대로 가져온다.** 이력 테이블은 여전히 UPDATE/DELETE 정책이 없고, 삭제는 소프트삭제다. | 이미 검증된 설계를 다시 검증할 필요가 없다. 이번 과제의 배점은 인증이지 CRUD 재설계가 아니다 |
| 6 | **다 막았다고 적지 않는다.** 확인 다섯 가지 이상을 통과해도 "⑥ 아직 못 막은 것"은 반드시 하나 이상 채운다. | "다 막았습니다"라고 적으면 오히려 재확인을 부른다(카드 5 troubleshooting 원문) |

> **경과 기간 원칙** — 실제 날짜 **5일**은 작업시간에 넣지 않는다. 3일차 진입 전(=2일차와 3일차
> 사이) 계획 규칙을 **하나만** 바꾼다. 날짜를 앞당기거나 지어내지 않는다.

---

## 1. 무엇으로 붙일지 고르고, 이유를 적기 (카드 1)

### 고른 것 — Supabase Auth (GoTrue), 이메일+비밀번호

이미 과제 6에서 Supabase Postgres + RLS를 쓰고 있다. **인증과 데이터 접근 제어가 같은
플랫폼에서 `auth.uid()`로 묶인다** — 이게 결정적 이유다. `@supabase/supabase-js`
(현재 `^2.58.0`, 과제 6과 동일 버전)의 `auth.signUp` / `auth.signInWithPassword` /
`auth.signOut` / `auth.onAuthStateChange`만으로 가입·로그인·로그아웃·세션 유지가 끝난다.

### 검토했지만 고르지 않은 것

| 후보 | 고르지 않은 이유 |
|------|------------------|
| **직접 구현** (bcrypt + 세션 쿠키를 손으로 짬) | 비밀번호 저장·세션 발급·만료·재설정을 전부 직접 검증해야 해서 실수 여지가 크다. 이 과제의 배점은 "많이 쓰이는 것을 잘 골랐는지"이지 인증을 재발명했는지가 아니다(카드 1 troubleshooting 원문) |
| **Auth.js(NextAuth)** | 서버 세션/미들웨어 전제가 강해 지금의 Vite SPA + 정적 배포 구조와 맞지 않는다. 붙이려면 서버를 새로 두어야 하는데, 과제 6부터 지켜온 "서버 없이 브라우저에서만" 원칙이 깨진다 |

### 데이터 이전 (T07-C100)

과제 6의 "전체 내보내기"(이미 있음)로 받은 JSON을 새 프로젝트에서 로그인한 **내 계정**으로
**가져오기**한다. 가져오기는 반드시 로그인 상태의 클라이언트가 실행해 각 행에 `auth.uid()`가
자동으로 찍히게 한다 — service_role 키로 대신 꽂아 넣지 않는다(원칙 4).

### 통과 기준과의 대응

| 통과 기준 | 어떻게 만족하는가 |
|-----------|------------------|
| T07-C91·92 | "Supabase Auth(GoTrue), `@supabase/supabase-js` 2.58.x" — 설명서 ①에 기록 |
| T07-C93 | 위 "검토했지만 고르지 않은 것" 표를 설명서 ②에 그대로 옮김 |
| T07-C94~97 | `AuthForm`(가입/로그인) + `AuthGate`(세션 없으면 로그인 화면만 렌더) |
| T07-C98 | Supabase Auth가 이메일 중복 가입을 기본으로 거절 — 실제 응답(오류 메시지)을 캡처 |
| T07-C99 | GoTrue의 기본 오류 메시지가 "Invalid login credentials"로 통일됨 — 계정 존재 여부를 흘리지 않는지 실제로 확인(둘 다 같은 문구인지 검사로 대조) |
| T07-C100 | 위 데이터 이전 절차 + 이전 전후 건수 대조표 |

---

## 2. 비밀번호를 어떻게 맡아 두는지 보이기 (카드 2)

### 고른 방법 — bcrypt (Supabase Auth 내장)

GoTrue는 비밀번호를 **bcrypt**로 해시해 `auth.users.encrypted_password`에 저장한다.
**직접 고른 것이 아니라 인증 서비스가 이미 그렇게 하고 있다는 사실 자체가 카드 2의 답**이다
— 원칙 2. 그래서 여기서 만드는 것은 알고리즘이 아니라 **증거**다.

### 남길 것 (그대로 실행 순서)

1. 테스트 계정 하나를 만들고, Supabase SQL 편집기에서 `select id, email, encrypted_password from auth.users where email = '<테스트 계정>';`을 실행해 **해시값 그대로**(`$2a$10$…`)를 설명서에 옮긴다. 입력한 글자가 안 보이는 걸 보인다.
2. **같은 비밀번호**로 계정을 하나 더 만들고 두 해시값을 나란히 놓아 **서로 다름**을 보인다(bcrypt는 계정마다 다른 salt를 자동으로 씀).
3. 로그인 요청·응답을 네트워크 탭에서 캡처해 **본문에 비밀번호 원문이 없는지** 확인하고, 있다면(요청 본문에는 당연히 평문이 실린다 — 그건 정상이다) **응답과 서버 로그 쪽에 원문이 없는지**를 본다. 캡처를 설명서에 붙일 때 요청 본문의 비밀번호 자리는 `••••••••`로 가린다.

### 실제로 확인한 증거 (2026-09-02, plandosee-auth Supabase 프로젝트)

검사 18·22가 만든 스크래치 계정 A·B는 **똑같은 비밀번호**(`CheckPass!23456`)로 가입했다. SQL 편집기에서
`select email, id, encrypted_password, created_at from auth.users where email like 'pds-auth-check-%@example.com' order by created_at desc limit 6;`를
실행해 얻은 값:

| 계정 | user id | encrypted_password |
|------|---------|---------------------|
| pds-auth-check-a-1788316813572@example.com | 3f36b1c9-58f9-4eb9-b31b-a8b0d7566cfc | `$2a$10$/Rpbk76a5Spss.LKsR/PnO48cRQhqQ3NKkmSEY6N/iM/IQX6cHGBm` |
| pds-auth-check-b-1788316813572@example.com | 0ef5b2d6-6cf4-4064-ac06-0998aedf5a9b | `$2a$10$TXosJIzyWWWWoRQwW8Iw9uci/oJh/FV4EUGRyZZ2Np8sM2IdeTcMa` |

둘 다 `$2a$10$…` 포맷(bcrypt, cost factor 10)이고, **같은 비밀번호인데도 해시가 완전히 다르다** — 각 계정마다 bcrypt가 무작위 salt를 자동으로 붙였다는 뜻이다. 비밀번호는 이 코드베이스 어디에도 직접 저장·비교하는 로직이 없고, 전부 Supabase Auth(GoTrue)에 위임했다.

| 통과 기준 | 대응 |
|-----------|------|
| T07-C101·102 | "bcrypt, Supabase Auth가 기본으로 사용 — 직접 고르지 않고 검증된 서비스에 위임" |
| T07-C103·104 | 위 1·2번 캡처 |
| T07-C105·106 | 위 3번 캡처. 요청 본문의 비밀번호는 가림 처리 |
| T07-C107 | "비밀번호 저장은 Supabase Auth(GoTrue)에 전적으로 위임했고 직접 구현한 부분이 없다"를 명시 |

---

## 3. 들어온 사람을 어떻게 기억하는지 보이기 (카드 3)

### 세션 방식 — JWT 액세스 토큰 + 리프레시 토큰

Supabase Auth는 로그인 성공 시 **JWT 액세스 토큰**(기본 만료 1시간)과 **리프레시 토큰**을
내려주고, `supabase-js`가 이를 브라우저 `localStorage`(`sb-<project-ref>-auth-token`)에
저장해 자동으로 갱신한다. **URL 쿼리에는 실리지 않는다**(이메일+비밀번호 흐름은 리다이렉트가
없다 — OAuth였다면 확인이 필요했을 부분).

### 확인 순서 (카드 3의 "성공 응답 vs 거절 응답")

1. 로그인 상태에서 `내 계획 목록 조회` 요청 1회 → 200과 목록을 캡처.
2. `supabase.auth.signOut()` 호출 → **같은 요청을 같은 액세스 토큰으로 다시** 보냄(브라우저가 아니라 저장해 둔 토큰 문자열로 직접 재전송) → 캡처.
3. 두 요청이 **같은 주소·같은 방식**이고 다른 건 로그아웃 여부뿐임을 캡처 메타데이터(요청 URL·메서드)로 보인다.
4. 액세스 토큰의 `exp` 클레임(JWT를 디코드하면 나온다)을 읽어 "발급 후 몇 분 뒤 만료"를 적는다.
5. 비밀번호를 바꾼 뒤 이전 리프레시 토큰으로 갱신을 시도해 거절되는지 확인(GoTrue가 비밀번호 변경 시 기존 리프레시 토큰을 무효화한다).
6. 토큰 문자열은 캡처에 옮길 때 앞 8자만 남기고 나머지는 가린다(`eyJhbGciOi…`처럼).

### 로그아웃 뒤 액세스 토큰 재사용 — 실제로 막은 방법 (2026-09-02)

처음에는 2번을 "401/403 캡처"로 적었는데, 실제로 돌려 보니 **로그아웃 뒤에도 200이 그대로
나왔다** — JWT는 stateless라 `signOut()`이 리프레시 토큰만 지우고, 액세스 토큰 자체는
서명이 `exp`(1시간)까지 계속 "유효"하기 때문이다. `auth.uid()`만 보는 RLS로는 이걸 막을 수 없다.

**해결**: Supabase Auth는 로그인마다 `auth.sessions`에 세션 행을 하나 만들고, `signOut()`이
그 행을 지운다. 그리고 액세스 토큰(JWT)에는 그 세션을 가리키는 `session_id` 클레임이 이미
들어 있다. 그래서 RLS 정책마다 "이 토큰의 `session_id`가 지금도 `auth.sessions`에 살아
있는가"를 추가로 검사하면, 로그아웃한 순간 그 행이 사라지므로 **토큰 서명은 여전히 유효해도
자료는 하나도 안 돌아온다**:

```sql
create or replace function session_is_active()
returns boolean
language sql
security definer
set search_path = auth, public
stable
as $$
  select exists (
    select 1 from auth.sessions where id = (auth.jwt()->>'session_id')::uuid
  );
$$;
grant execute on function session_is_active() to authenticated;

-- 기존 auth.uid() = user_id 조건마다 and session_is_active()를 덧붙인다.
alter policy plans_select on plans using (auth.uid() = user_id and session_is_active());
-- (plans/plan_revisions/tasks/execution_records/review_notes 전부 동일)
```

`authenticated` 역할은 `auth.sessions`에 직접 SELECT 권한이 없어 `security definer` 함수로
우회한다. SELECT는 RLS의 `using`이 거짓이면 그냥 그 행이 결과에서 빠질 뿐이라 **상태 코드는
여전히 200**이다 — "거절"의 증거는 상태 코드가 아니라 **응답 본문이 빈 배열**이라는 점이다.
검사 23을 이 사실에 맞게 다시 썼다: 로그인 상태에서는 내 계획이 목록에 잡히고, 로그아웃
뒤 같은 토큰으로는 같은 요청이 0건을 돌려준다.

| 통과 기준 | 대응 |
|-----------|------|
| T07-C108 | "JWT 액세스 토큰 + 리프레시 토큰(Supabase Auth 발급)" |
| T07-C109·110 | 위 1·2·3번 캡처 |
| T07-C111 | 액세스 토큰 `exp` 값과 "발급 후 1시간" 명시 |
| T07-C112 | "쿼리 파라미터에 없음"을 네트워크 탭 캡처로 증명 |
| T07-C113 | JWT 서명 비밀키는 Supabase 프로젝트 설정(대시보드)에만 있고 클라이언트 코드·번들·Git에는 없음 — 카드5의 빌드 산출물 정적 검사(과제 6의 검사 16을 그대로 확장)로 재확인 |
| T07-C114 | 위 5번 캡처 |
| T07-C115 | 모든 캡처의 토큰 문자열은 앞 8자 + `…`로 가림 |

---

## 4. 남의 자료가 안 열리는 것을 보이기 (카드 4)

### 구조로 막는다 — RLS + 트리거

```sql
-- 각 표에 소유자 칼럼. 기본값이 아니라 트리거로 못박아 클라이언트 입력을 신뢰하지 않는다.
alter table plans add column user_id uuid not null references auth.users(id);

create or replace function stamp_owner()
returns trigger language plpgsql security definer as $$
begin
  new.user_id := auth.uid();  -- 요청 본문에 뭐가 왔든 무시하고 항상 이걸로 덮어쓴다
  return new;
end; $$;

create trigger plans_stamp_owner before insert on plans
  for each row execute function stamp_owner();

drop policy if exists plans_select on plans;
create policy plans_select on plans for select using (auth.uid() = user_id);
create policy plans_insert on plans for insert with check (auth.uid() = user_id);
create policy plans_update on plans for update using (auth.uid() = user_id) with check (auth.uid() = user_id);
-- DELETE 정책 없음 — 과제 6과 동일하게 소프트삭제만
```

`tasks`·`execution_records`·`review_notes`도 같은 패턴(각자 `user_id` + `stamp_owner`
트리거 + `auth.uid() = user_id` 정책)을 반복한다. 자식 표를 부모의 소유자로 조인해서 검사하는
대신 **모든 표에 `user_id`를 직접 두는 쪽**을 고른다 — 정책이 짧고 빨라 실수할 자리가 준다.

### 실제로 두들겨 보는 순서 (양방향)

1. 계정 A·B를 만들고 각각 계획·할일 몇 건씩 넣는다(비밀번호는 설명서에 안 적는다).
2. A로 로그인해 B의 계획 id를 직접 넣어 **조회·수정·삭제**를 요청 → 세 응답 모두 거절(403 또는 404) 캡처.
3. 반대로 B로 로그인해 A에 대해 같은 세 가지.
4. 요청의 **주소·헤더·본문 어디에 B의 계정 정보를 적어 보내도** A로 로그인한 세션이면 A의 자료만 돌아오는지 확인.
5. 로그아웃 상태로 계획 목록 API를 직접 호출 → 거절.
6. **목록 조회**도 한 건 조회와 같은 정책을 타는지 확인 — 목록 API 응답 전체에서 상대 계정의 id가 하나도 없는지 텍스트로 검색해 "0건"을 캡처.
7. 거절 시도 전후로 상대 계정의 자료 건수를 세어 **바뀌지 않았음**을 대조.
8. 위 거절이 나오는 소스 위치(RLS 정책이 걸린 `supabase/schema.sql`의 정확한 줄)를 명시.

| 통과 기준 | 대응 |
|-----------|------|
| T07-C116 | 2·3번 준비 단계 캡처(비밀번호 제외) |
| T07-C117~120 | 2·3번 결과 캡처(양방향 읽기/수정/삭제) |
| T07-C121 | 403/404 응답 코드를 캡처에 명시 |
| T07-C122 | 7번 대조표 |
| T07-C123 | 4번 캡처 |
| T07-C124 | 5번 캡처 |
| T07-C125 | 6번 캡처 |
| T07-C126 | `plans`(및 각 표) RLS 정책 정의 줄 번호를 설명서 ③에 인용 |

---

## 5. 설명서로 묶고, 5일 써 보기 (카드 5)

### 인증 구현 설명서 여섯 항목 — 무엇을 채우는지 미리 정한다

| 항목 | 채울 내용 | 비워 두면 안 되는 이유 |
|------|-----------|------------------------|
| ① 무엇으로 붙였나 | Supabase Auth(GoTrue), email+password, `@supabase/supabase-js` 버전 | T07-C91·92 |
| ② 왜 그걸 골랐나 | 1절의 "검토했지만 고르지 않은 것" 표 | T07-C93 |
| ③ 어디를 어떻게 고쳤나 | 가입·로그인·로그아웃·자료조회 네 흐름이 지나는 소스 위치(파일:함수) | T07-C128 |
| ④ 안 열리는 것을 확인한 기록 | 카드 3·4의 확인 다섯 가지, 성공/거절 나란히 | T07-C129 |
| ⑤ AI와 나 | 기존 3줄 형식 재사용 | — |
| ⑥ 아직 못 막은 것 | 아래 표를 그대로 옮기되 **실제 구현 후 갱신** | T07-C130 |

### ⑥에 미리 채워 둘 후보 (구현하며 실제로 막았으면 지운다)

> **로그아웃 뒤 액세스 토큰 재사용**은 처음엔 이 표에 있었지만, `session_is_active()`(2절 참고)로
> 실제로 막아서 지웠다 — "다 막았다"를 남발하지 않되, 실제로 막은 것까지 여기 남겨 두지는 않는다.

| 아직 못 막은 것 | 왜 위험한가 |
|------------------|-------------|
| 무차별 대입(brute-force) 방지 없음 | 같은 계정에 비밀번호를 계속 시도해도 막는 장치가 없어 시간을 들이면 뚫릴 수 있다 |
| 비밀번호 재설정 이메일 흐름 미구현 | 비밀번호를 잊으면 계정을 복구할 방법이 없다(시간이 부족해 다음으로 미룸) |
| 2단계 인증 없음 | 비밀번호 하나만 뚫리면 끝이다 |
| 로그인 시도 로그 없음 | 누가 언제 실패했는지 남지 않아 이상 징후를 못 본다 |

### 5일 사용 — 지표는 1일차에 딱 한 번 정한다

> **질문**: 실제로 일한 시간이 계획한 시간과 얼마나 어긋나는가?
> **지표**: 그날 실행기록의 실제 시간 합계 · **단위: 분**
> **계산 규칙**: 그날 KST 날짜에 속하는 실행기록의 `actual_minutes`를 전부 더함. 값이 비면 그
> 기록은 빼고 더함(모수에서 제외). 같은 날 여러 건이면 **합산**한다(중복이 아니라 정상).
> 유난히 큰 값(그날 다른 기록 평균의 3배 이상)은 지우지 않고 화면에 **"이상치일 수 있음"** 표시만
> 붙여 집계에는 그대로 포함한다. 분 단위는 반올림하지 않고, 평균을 낼 때만 소수 첫째자리까지
> 반올림한다. **주 시작 요일은 월요일**(Asia/Seoul, 과제 6과 동일 관례).

이 문단을 **1일차 기록 전에 그대로 고정**한다 — 나중에 바꾸지 않는다(T07-C04~08, T07-C23~27).

### 계획 규칙 변경 — 2일차 뒤, 3일차 앞

과제 6 카드 5와 같은 원칙: **미리 정하지 않는다.** 1~2일차를 실제로 써 보고 반복해서 걸리는
지점을 하나 고른다. 지금 예상되는 후보(확정 아님):

1. 마감일을 예상 시간 그대로 잡아 항상 촉박함 → **예상 시간의 1.5배**를 마감 여유로 둔다
2. 하루에 할 일을 너무 많이 잡아 완료율이 낮음 → 하루 계획 시간 총합에 상한을 둔다

바꾼 **시각**과 **이유**를 기록하고, 규칙 변경 전(1~2일차)·후(3~5일차)를 **같은 지표·같은
단위·같은 계산 규칙**으로 나란히 비교한다. 나빠졌어도 감추지 않는다(troubleshooting 원문).

### 계정 삭제 (T07-C134)

Auth 사용자 레코드 자체를 지우려면 `service_role` 키가 필요한 관리자 API라 **클라이언트에서
직접 호출할 수 없다**(원칙 위반). 그래서:

- "내 계정 삭제" 버튼은 로그인한 클라이언트 권한으로 **내 소유 행(계획·이력·할일·실행기록·고칠점)을
  전부 지운다**(RLS가 허용하는 범위 — 본인 것만 지워진다).
- 화면에 "가입 정보(이메일)는 별도 절차로 삭제됩니다"를 명시해 **"지워진다는 안내"**로
  T07-C134의 OR 조건을 만족한다. ⑥에도 "Auth 계정 레코드 자체의 하드 삭제는 서버(Edge
  Function) 없이는 못 만들었다"를 정직하게 적는다.

### 그 외 카드5 통과 기준

| 통과 기준 | 대응 |
|-----------|------|
| T07-C132 | 화면 합계·평균을 5일치 손 계산과 대조하는 표를 설명서에 첨부 |
| T07-C133 | 과제 6의 "전체 내보내기"를 그대로 재사용(이미 있음) |
| T07-C03 | 로그인 화면 자체는 비로그인으로 열리되 로그인 뒤 자료는 비공개 — `AuthGate`가 이를 보장 |

---

## 6. 구조

```
plandosee-auth/                 ← plandosee/를 T06 최종 커밋에서 복제해 시작
├─ src/
│  ├─ api/
│  │  ├─ client.js              Supabase 클라이언트(새 프로젝트 URL/키) + auth 노출
│  │  ├─ auth.js                signUp·signIn·signOut·getSession·onAuthStateChange  [신규]
│  │  ├─ migrateFromT06.js      T06 내보내기 JSON → 로그인 계정으로 가져오기        [신규]
│  │  ├─ plans.js / tasks.js / executionRecords.js / reviews.js / exportAll.js      (그대로, RLS만 owner-scoped)
│  ├─ components/
│  │  ├─ AuthGate/               세션 없으면 로그인 화면만 렌더                     [신규]
│  │  ├─ AuthForm/                가입 · 로그인 폼, 오류 문구 통일                  [신규]
│  │  ├─ AccountSection/          계정 삭제 버튼 + 안내                            [신규]
│  │  ├─ PlanSection · TaskSection · ExecutionSection · ReviewSection · ExportSection  (과제 6 그대로)
│  ├─ pages/Diary/                최상위. <AuthGate><Diary/></AuthGate> 로 감쌈
├─ supabase/schema.sql            user_id + stamp_owner 트리거 + auth 기반 RLS로 재작성
├─ contracts/pds-schema-v3.json   스키마 계약 갱신(버전 올림)
└─ tools/
   ├─ check.mjs                  카드1~4의 인증·격리 검사 추가(아래 8절)
   ├─ capture.mjs                로그인 화면 · 거절 응답 · 토큰 만료 캡처
   └─ report.mjs                 "인증 구현 설명서" 6항목 구조로 새로 작성
```

과제 6의 CDP 검사·촬영·보고서 뼈대는 그대로 옮긴다 — 새로 배우는 비용을 쓰지 않는다.

---

## 7. 화면 구성

1. **로그인 화면** (비로그인 시 첫 화면, `AuthGate`가 강제) — 가입/로그인 전환, 오류 문구 통일
2. 로그인 뒤: 과제 6과 동일한 화면(계획·할일·실행기록·돌아보기·내보내기) + 헤더에 **로그아웃**·**내 계정** 버튼
3. **계정 섹션** — 로그아웃, 계정 삭제(확인 1회), 삭제 안내 문구

---

## 8. 검사 목록 (과제 6의 17개에 이어 번호를 매긴다)

| # | 카드 | 검사 |
|---|------|------|
| 18 | 카드1 | 가입 → 로그인 → 로그아웃이 순서대로 된다 |
| 19 | 카드1 | 같은 이메일로 두 번 가입하면 거절된다 |
| 20 | 카드1 | 존재하지 않는 계정과 비밀번호만 틀린 계정의 오류 문구가 같다 |
| 21 | 카드1 | 로그인하지 않고 자료 화면 주소를 열면 로그인 화면이 나온다 |
| 22 | 카드2 | 같은 비밀번호로 만든 두 계정의 저장된 해시가 서로 다르다 |
| 23 | 카드3 | 로그인 상태 요청은 200(내 자료 있음), 로그아웃 뒤 같은 토큰으로 요청해도 200이지만 0건(session_is_active) |
| 24 | 카드3 | 액세스 토큰이 URL 어디에도 실리지 않는다 |
| 25 | 카드3 | 비밀번호 변경 후 이전 리프레시 토큰이 더는 통하지 않는다 |
| 26 | 카드4 | 계정 A가 계정 B의 계획을 id로 직접 읽으면 거절된다(반대 방향도) |
| 27 | 카드4 | 계정 A가 계정 B의 계획을 수정·삭제하려 하면 거절된다(반대 방향도) |
| 28 | 카드4 | 목록 조회 응답에 상대 계정의 행이 0건이다 |
| 29 | 카드4 | 요청 본문에 남의 user_id를 적어 보내도 내 자료만 저장·조회된다(트리거 검증) |
| 30 | 카드5 | 빌드 산출물에 Supabase URL/anon key 외의 비밀값(JWT 서명 키·service_role)이 없다 |
| 31 | 카드5 | 화면의 5일 합계·평균이 손 계산과 같다 |

---

## 9. 카드별 구현 순서

| 카드 | 만드는 것 | 통과를 보이는 검사 |
|------|-----------|---------------------|
| 0 | `plandosee/` 복제, 새 Supabase·Vercel 프로젝트 생성 | — |
| 1 | Supabase Auth 연동, `AuthGate`/`AuthForm`, 데이터 이전 스크립트 | 18·19·20·21 |
| 2 | (해시는 서비스가 대신함) 증거 캡처만 | 22 |
| 3 | 세션 만료·로그아웃 무효화 확인 | 23·24·25 |
| 4 | `user_id` 컬럼·트리거·RLS 재작성, 양방향 격리 시험 | 26·27·28·29 |
| 5 | 설명서 6항목 작성, 5일 실사용 + 규칙 변경, 계정 삭제 | 30·31 |

카드 4의 격리 시험 자료(계정 A·B)는 **카드 1을 끝낸 직후에 미리 만든다** — 5일을 기다리는
동안 검사할 게 없으면 날짜를 지어내고 싶어진다(과제 6에서 배운 것 그대로).

---

## 10. 검증 안내서 골격

```
어디로 가나요: (새 결과물 주소 — 로그인 화면)

무엇을 하나요 (3단계)
1) 새 계정으로 가입한다
2) 로그인해 계획 하나를 만든다
3) 로그아웃한 뒤 같은 자료 화면 주소를 다시 연다

무엇이 보이면 통과인가요
1) 가입 직후 자동으로 로그인되거나 로그인 화면으로 이동한다
2) 만든 계획이 내 화면에만 보인다
3) 로그아웃 뒤에는 로그인 화면만 보이고 자료는 보이지 않는다

안 될 때
- 가입이 안 되면 이미 있는 이메일인지 오류 문구를 읽는다
- 로그인 후에도 로그인 화면이면 새로고침 한 번 해 본다(세션 반영 지연)
- 자료가 안 보이면 계정을 잘못 골랐는지(다른 계정으로 가입했는지) 확인한다
```

---

## 11. 미리 대비할 위험

| 위험 | 왜 생기나 | 막는 법 |
|------|-----------|---------|
| 화면에서만 숨기고 서버는 안 막음 | RLS 없이 클라이언트 필터링만 함 | 원칙 3 — 반드시 RLS. 검사 26·27·28이 API를 직접 두들겨 확인 |
| 요청 본문에 남의 ID를 적어 우회 | `user_id`를 클라이언트 입력값으로 저장 | 원칙 4 — `stamp_owner` 트리거가 항상 `auth.uid()`로 덮어씀. 검사 29 |
| 로그아웃해도 예전 토큰이 통함 | JWT가 stateless라 auth.uid()만 보면 signOut() 이후에도 서명이 exp까지 유효함 | RLS마다 session_is_active()로 auth.sessions에 그 세션이 살아있는지 같이 검사 — 로그아웃 즉시 자료가 0건이 됨(검사 23) |
| 목록은 막았는데 상세 조회가 뚫림(또는 반대) | 목록·단건이 서로 다른 코드 경로 | 같은 RLS 정책이 두 경로 모두를 통제하는지 검사 26·28로 각각 확인 |
| 비밀번호·토큰이 캡처에 그대로 남음 | 화면 캡처를 그대로 붙임 | 원칙 1의 부속 — 캡처마다 가림 처리를 체크리스트로 확인 후 붙인다 |
| 데이터 이전 중 남의 자료가 섞임 | 이전 스크립트가 `user_id`를 직접 지정 | 반드시 로그인한 클라이언트로 실행해 트리거가 `auth.uid()`를 찍게 한다 |
| ⑥이 비어 있음 | 다 막았다고 적고 싶은 유혹 | 4절 표를 구현 시작 전에 먼저 적어 두고, 실제로 막은 것만 지운다 |

---

## 12. 지금 정해야 할 것

1. **새 폴더 이름** — 제안: `plandosee-auth`. `plandosee/`는 그대로 두고 복제한다.
2. **새 Supabase 프로젝트** — 과제 6 프로젝트(`plandosee`)와 절대 공유하지 않는다. 새로 만들고
   스키마도 새로(`user_id`·트리거·auth 기반 RLS 포함) 올린다.
3. **새 Vercel 프로젝트/도메인** — 예: `aleph-pds-auth`. 과제 6 주소(`aleph-pds.vercel.app`)는
   그대로 둔다.
4. **인증 방식 확정** — 위 제안은 Supabase Auth(email+password). 다른 인증 서비스로 바꿔도
   되지만, 검토했다가 고르지 않은 이유를 반드시 한 문장으로 남길 수 있어야 한다.
5. **5일 지표·계획 규칙 변경** — 지표·단위·계산 규칙은 지금 정해 1일차부터 고정한다. **어떤
   규칙을 바꿀지는 실제로 1~2일 써 보고 정한다** — 미리 정하지 않는다.
6. **계정 삭제 범위** — 위 제안은 "내 데이터 행만 하드 삭제 + Auth 레코드는 별도 절차 안내".
   Edge Function까지 만들어 Auth 레코드도 완전히 지울지는 시간 여유에 따라 결정한다.
