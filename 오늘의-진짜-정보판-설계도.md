# 오늘의 진짜 정보판 — 설계도

날씨·환율·버스·게임 서버 상태 등 실제로 변하는 데이터 한 가지를 값·출처·조회 시각과 함께 보여주는 개인 정보판.

---

## 0. 이 설계도의 기준

과제 카드 5개는 아래 **세 가지 설계 원칙** 중 하나에서 따라 나온다. 구현 중 판단이 갈리면 이 세 줄로 되돌아온다.

| # | 원칙 | 이 원칙이 막아주는 실패 |
|---|------|------------------------|
| 1 | **데이터를 가져오는 코드는 `fetchSnapshot()` 하나뿐이다.** 화면·저장·비교는 전부 이 함수의 반환값만 쓴다. | 화면값과 저장값이 서로 다른 곳에서 따로 계산돼 어긋나는 문제 (카드 1·5의 핵심) |
| 2 | **"마지막 정상값"과 "지금 시도"는 절대 같은 자리에 두지 않는다.** 실패해도 정상값 자리를 덮어쓰지 않는다. | 장애가 나면 값이 사라지거나, 장애 5종이 전부 같은 문구로 보이는 문제 (카드 3의 핵심) |
| 3 | **저장 키는 "기준 시간대로 변환한 날짜 문자열"뿐이다.** 저장 전 반드시 그 키가 있는지부터 조회한다. | 같은 날 여러 번 실행했을 때 기록이 중복되거나, 자정 근처에서 날짜가 어긋나는 문제 (카드 4의 핵심) |

> **구현 확정 (2026-08-24)** — 이 설계도는 `today-dashboard/`로 구현됐다. 구현하면서 §6·§7이 아래와 같이 바뀌었고, 본문에 반영돼 있다.
> 어제 값을 하루 기다려 얻는 대신 **같은 API 응답의 `past_days`로 함께 받아온다.** 대신 기록마다 `origin`(직접 조회 / 출처의 지난 기록)을 남겨 "언제 본 값인지"를 화면이 속이지 않게 한다.

---

## 1. 기술 스택과 구조

기존 `card-matching-game`·`zzal-studio`와 동일한 스택을 쓴다. 새로 배울 것을 늘리지 않고 "실시간 데이터·비밀값·장애 처리"라는 이 과제 고유의 문제에 집중하기 위해서다.

- React 19 + Vite, Emotion(`styles.js` 분리 패턴), Vercel 배포
- 데이터 공급자로 **키가 필요 없는 공개 API**를 1순위로 고른다 (근거는 §5). 이 경우 서버 없이 브라우저에서 바로 호출한다.
- 만약 반드시 키가 필요한 API를 골랐다면, Vercel 서버리스 함수 1개(`api/snapshot.js`)를 프록시로 둔다. 이 설계도는 기본값을 "키 불필요"로 잡고, 서버리스 프록시는 §5에서 대안으로만 다룬다.

```
today-dashboard/
├─ index.html
├─ vercel.json                    # SPA 리라이트
├─ api/
│  └─ snapshot.js                 # (선택) 키가 필요한 공급자를 쓸 때만 생성
└─ src/
   ├─ main.jsx
   ├─ App.jsx
   ├─ pages/
   │  └─ Dashboard/                # 메인 화면 (현재값 + 상태 + 기록)
   ├─ components/
   │  ├─ CurrentValueCard/         # 값·단위·출처 링크·조회 시각
   │  ├─ StatusBadge/              # 정상 / 오래된 데이터 / 장애별 문구
   │  ├─ FailurePanel/             # 장애 5종 강제 재현 버튼 (?debug=1)
   │  ├─ HistoryList/              # 날짜별 기록 목록
   │  └─ DiffCard/                 # 어제 대비 변화
   ├─ providers/
   │  ├─ index.js                  # 현재 사용 공급자 선택
   │  └─ weatherOpenMeteo.js        # 기본 후보 공급자 (예시)
   ├─ core/
   │  ├─ fetchSnapshot.js           # 원칙 1 — 단일 진실 원천
   │  ├─ dashboardState.js          # 원칙 2 — ok/lastGood/error 분리
   │  └─ failureSim.js              # 장애 5종 모의실험
   ├─ storage/
   │  └─ history.js                 # 원칙 3 — 날짜 키 저장소
   ├─ utils/
   │  └─ timezone.js                 # 기준 시간대 변환
   └─ constants/
```

---

## 2. 데이터 공급자 선택 (카드 1)

특정 API를 강제하지 않는다. 대신 아래 인터페이스를 만족하면 무엇이든 꽂을 수 있게 한다.

```js
// providers/weatherOpenMeteo.js (예시 — 실제 선택은 자유)
export const provider = {
  key: 'weather-openmeteo',
  label: '서울 현재 기온',
  buildSourceUrl: () =>
    'https://api.open-meteo.com/v1/forecast?latitude=37.57&longitude=126.98&current=temperature_2m',
  normalize: (json) => ({
    value: json.current.temperature_2m,
    unit: '°C',
    itemLabel: '서울 현재 기온',
    rawFetchedAt: json.current.time,       // 출처가 밝힌 갱신 시각
  }),
};
```

**후보 비교** — "출처 주소를 누르면 원자료 페이지가 열린다"(완료 기준)를 가장 쉽게 만족하는 것은 **API 요청 URL 자체가 브라우저에서 그대로 JSON 원자료 페이지로 열리는** 공급자다.

| 후보 | 키 필요 | 원자료 페이지 | 추천도 |
|------|---------|---------------|--------|
| 날씨 (Open-Meteo) | 불필요 | 요청 URL이 곧 JSON 원자료 | ★★★ 1순위 — 카드 2가 사실상 자동 통과 |
| 환율 (공개 환율 API) | 대개 불필요 | 요청 URL이 곧 JSON 원자료 | ★★★ 동급 |
| 버스 도착 정보 (공공데이터포털) | 대개 필요 | 신청키 발급 필요, 웹페이지는 별도 | ★☆☆ — §5의 프록시 필요 |
| 게임 서버 상태 (예: Steam Status) | 불필요~일부 필요 | 상태 페이지는 있으나 API가 불안정 | ★★☆ |

**선택 기준**: 값·단위·갱신 시각이 응답에 명시돼 있고, 요청 URL을 그대로 열어도 개인정보나 비밀값이 안 보이는 공급자를 고른다. 이 조건을 만족하면 카드 2의 "비밀값 0건"이 설계 단계에서부터 성립한다.

`normalize()`의 반환값이 이 정보판 전체가 다루는 유일한 데이터 형태(Snapshot)다.

```ts
Snapshot = {
  value: number,
  unit: string,
  itemLabel: string,
  sourceUrl: string,     // buildSourceUrl() 결과, 화면 클릭 링크
  fetchedAt: string,     // ISO, 내가 조회를 완료한 시각
  rawFetchedAt: string,  // 출처가 밝힌 갱신 시각 (있으면)
}
```

---

## 3. 상태 모델 (카드 3의 심장 — 원칙 2)

"지금 시도"와 "마지막 정상값"을 같은 변수에 절대 같이 담지 않는다.

```js
// core/dashboardState.js
{
  lastGood: Snapshot | null,     // 성공했을 때만 갱신. 실패해도 절대 건드리지 않음
  attempt: {
    status: 'idle' | 'loading' | 'ok' | 'error',
    errorKind: null | 'timeout' | 'auth' | 'rateLimit' | 'offline' | 'formatChange',
    errorMessage: string | null,
    at: string,                  // 이 시도가 끝난 시각
  },
}
```

화면 규칙은 이 상태만으로 완전히 결정된다.

| attempt.status | lastGood 있음 | 화면 |
|---|---|---|
| ok | — | 정상 상태, `attempt`의 값으로 표시, "오래된 데이터" 배지 없음 |
| loading | 있음 | 기존 lastGood 유지 표시 + 로딩 표시만 얹음 |
| error | 있음 | lastGood 값 + "오래된 데이터" 배지 + `errorKind`별 문구 + 마지막 정상 조회 시각 |
| error | 없음 | 빈 상태 문구 ("아직 정상값이 없습니다") + 다시 시도 버튼. **값이 있는 것처럼 꾸미지 않는다** |

`fetchSnapshot()`은 성공하면 `lastGood`을 교체하고 `attempt`를 `ok`로, 실패하면 `lastGood`은 그대로 두고 `attempt`만 `error`로 바꾼다. 이 두 줄만 지키면 카드 3의 통과 기준("현재 자료인지 오래된 자료인지 상태 문구로 구분")이 구조적으로 성립한다.

---

## 4. 장애 5종 모의실험 (카드 3)

실제 서버 장애를 기다릴 수 없으므로, `?debug=1`로만 보이는 `FailurePanel`에서 다음 요청 1회의 결과를 강제로 바꾼다. 실제 fetch는 그대로 나가되(또는 스킵하고), 응답을 가로채 아래 표대로 처리한다.

```js
// core/failureSim.js
export function withFailureMode(mode, realFetch) {
  switch (mode) {
    case 'timeout':      return neverResolve();               // AbortController가 5초 뒤 abort
    case 'auth':         return fakeResponse(401);
    case 'rateLimit':     return fakeResponse(429);
    case 'offline':       return Promise.reject(new TypeError('Failed to fetch'));
    case 'formatChange':  return fakeResponse(200, { unexpected: true });
    default:               return realFetch();
  }
}
```

| 장애 종류 | 재현 방법 | 화면 문구 | 마지막 정상값 처리 |
|---|---|---|---|
| timeout | `AbortController`로 5초 초과 시 강제 중단 | "응답이 지연되고 있어요 (제한시간 초과)" | 유지 + 오래된 데이터 배지 |
| 인증 실패 | 401/403 모의 응답 | "접근 권한에 문제가 있어요" | 유지 |
| 호출 제한 | 429 모의 응답 | "요청이 많아 잠시 제한됐어요" | 유지 |
| 오프라인 | fetch 자체가 reject되도록 모의 | "인터넷 연결을 확인해주세요" | 유지 |
| 응답 형식 변경 | 200이지만 `normalize()`가 기대하는 필드가 없는 JSON | "출처 응답 형식이 바뀌었어요 (확인 필요)" | 유지 |

각 장애 문구는 서로 다른 문자열 + 서로 다른 배지 색이어야 "구분된다"는 완료 기준을 만족한다.

`?fail=<장애 종류>`로 열면 **첫 조회부터** 실패한다. 마지막 정상값이 하나도 없는 상태를 그대로 보여주기 위한 것으로, "정상값이 없을 때 값이 있는 것처럼 표시하지 않는다"는 체크리스트 항목을 주소 하나로 재현할 수 있다. 버튼으로 재현하면 이미 정상값이 있어서 이 상태에 도달할 수 없다. "다시 시도" 버튼은 항상 `attempt`를 `loading`으로 초기화한 뒤 `fetchSnapshot()`을 다시 부른다 — 이전 `errorKind`가 남아 있는 채로 새 요청 결과를 덧그리면 안 된다.

정상값을 한 번도 저장하지 못한 상태에서 장애를 재현하면 §3의 "빈 상태" 규칙이 적용된다 (값이 있는 것처럼 보이면 안 됨).

---

## 5. 비밀값 관리 (카드 2)

**결정 트리:**

1. 1순위 — 키가 필요 없는 공개 API를 쓴다 (§2 추천 후보). 코드 어디에도 비밀값 자체가 없으므로 "브라우저·배포 파일·네트워크 주소·Git 기록에 비밀값 0건"이 설계상 자동으로 성립한다.
2. 반드시 키가 필요한 API라면 **브라우저에서 절대 직접 호출하지 않는다.** `api/snapshot.js` 서버리스 함수가 서버 쪽에서만 키를 쓰고, 클라이언트는 이 프록시 주소만 호출한다. 이 경우 "출처 주소"는 발급처가 제공하는 공개 문서/원자료 열람 페이지로 대체한다 (키가 노출되는 원본 API URL을 그대로 화면에 걸지 않는다).
3. 키는 `.env.local`(Git 미추적)과 Vercel 프로젝트 환경변수에만 둔다.

**검사 절차 (구현 완료 후 매번 반복):**

| 순서 | 검사 대상 | 방법 |
|---|---|---|
| 1 | 소스 코드 | 저장소 전체에서 키 문자열·`process.env` 오용 검색 |
| 2 | 빌드 결과물 | `vite build` 산출물(`dist/`)에서 키 문자열 검색 |
| 3 | 네트워크 요청 | 브라우저 개발자 도구 Network 탭에서 요청 URL·헤더·쿼리스트링 확인 |
| 4 | Git 기록 | `git log -p` 전체 이력에서 키 패턴 검색 (한 번이라도 커밋됐으면 이력에 영구히 남음) |
| 5 | 배포 파일 | 배포 주소에서 `view-source:` 또는 소스맵으로 키 노출 여부 확인 |

---

## 6. 일별 기록 저장소 (카드 4 — 원칙 3)

**기준 시간대는 KST(Asia/Seoul, UTC+9)로 명시**하고 화면과 기록 파일에 그대로 적는다.

```js
// utils/timezone.js
const TIMEZONE = 'Asia/Seoul';

export function dateKeyOf(isoString) {
  return new Intl.DateTimeFormat('en-CA', {
    timeZone: TIMEZONE, year: 'numeric', month: '2-digit', day: '2-digit',
  }).format(new Date(isoString)); // → 'YYYY-MM-DD'
}
```

```js
// storage/history.js
const KEY = 'today-dashboard.history.v1';

load()                     // 실패 시 [] + 경고 1회, 앱은 계속 동작
saveOnce(snapshot) {
  const dateKey = dateKeyOf(snapshot.fetchedAt);
  const items = load();
  if (items.some(r => r.dateKey === dateKey)) {
    return { ok: false, reason: 'duplicate' };   // 같은 날 재실행 → 저장 안 함, 기존 값 유지
  }
  items.push({ dateKey, value: snapshot.value, unit: snapshot.unit,
               fetchedAt: snapshot.fetchedAt, sourceUrl: snapshot.sourceUrl });
  saveAll(sortDesc(items));
  return { ok: true };
}
```

- 저장 식별값은 `${provider.key}:${dateKey}` 하나다.
- **저장 전에 반드시 `load()`로 기존 항목을 조회**한다 — 이 순서를 안 지키면 같은 날 여러 번 실행 시 중복이 생긴다.

### 서로 다른 날짜 2건을 하루 기다리지 않고 확보하는 방법

Open-Meteo는 `past_days=3` 파라미터로 **지난 날짜의 실제 관측값을 같은 응답에 함께** 돌려준다. 어제 값을 얻으려고 하루를 기다릴 이유가 없다. 다만 정직성 문제가 하나 생긴다 — 그 값의 **조회 시각은 오늘**이다.

그래서 기록마다 `origin`을 남기고 화면에서 배지로 구분한다.

| origin | 뜻 | 화면 표기 |
|---|---|---|
| `live` | 오늘 실제로 조회해 저장한 값 | `직접 조회` (초록) |
| `backfill` | 같은 응답에 들어 있던 지난 날짜 값 | `출처의 지난 기록` (회색) |

첫 조회에서 기록이 2건 미만이면 자동으로 지난 날짜를 채운다. 그래야 저장소가 빈 채점자가 페이지를 처음 열어도 비교 화면이 바로 보인다.

### 기준 시각을 09시로 고정하는 이유

날짜별 기록의 값은 "내가 조회한 순간의 값"이 아니라 **매일 09시 KST의 값**이다. 두 날짜를 같은 시각끼리 비교해야 변화량이 의미를 갖고, 채점자가 원자료 JSON에서 같은 두 숫자를 직접 찾아 대조할 수 있다. 맨 위 카드의 **현재값**은 이와 별개로 지금 이 순간의 값이다.

**아직 오지 않은 시각은 저장하지 않는다.** Open-Meteo는 예보 API라 오늘 남은 시간의 값도 함께 주는데, 이것을 관측값처럼 저장하면 없는 값을 지어내는 것이 된다. `hourly.time`을 `current.time`과 비교해 걸러낸다 — 둘 다 시간대 표기가 없는 같은 형식의 KST 문자열이라 **사전순 비교가 곧 시간순 비교**다. 브라우저 시계가 아니라 출처의 시계를 기준으로 삼는다는 이점도 있다.

---

## 7. 어제 대비 비교 (카드 5)

```js
// core/computeDiff.js
export function computeDiff(recordsSortedDesc) {
  const [latest, previous] = recordsSortedDesc;
  if (!latest || !previous) return null;              // 자료 부족 → 비교값 표시 안 함
  if (latest.unit !== previous.unit) return null;       // 단위 다름 → 비교값 표시 안 함
  const delta = round(latest.value - previous.value);
  return {
    delta,
    direction: delta > 0 ? '증가' : delta < 0 ? '감소' : '변화없음',
    unit: latest.unit,
    from: previous, to: latest,
  };
}
```

**대조표(제출 증빙)**: 두 날짜 각각에 대해 `원자료 값 / 저장값 / 계산 입력값 / 화면값` 4열을 손으로 채워 넣고, `computeDiff`의 결과와 손계산이 같은지 확인한다. 반올림은 화면 표시 직전에 한 번만 한다(저장값·계산 입력값은 원본 정밀도 유지) — 그렇지 않으면 "화면값과 저장값이 다르다"는 전형적 결함이 생긴다.

---

## 8. 화면 구성

```
┌───────────────────────────────────────┐
│ 오늘의 진짜 정보판 — 이 정보판은 ___를 확인하기 위한 것 │
├───────────────────────────────────────┤
│  [CurrentValueCard]                     │
│  값  단위        (StatusBadge: 정상/오래된 데이터/장애문구) │
│  출처: <sourceUrl 링크>                  │
│  조회 시각: HH:mm:ss (KST)               │
├───────────────────────────────────────┤
│  [DiffCard] 어제 대비: ▲ +1.2°C           │
├───────────────────────────────────────┤
│  [HistoryList] 날짜별 기록 (중복 없음)     │
│   2026-08-23  21.4°C                    │
│   2026-08-24  22.6°C                    │
└───────────────────────────────────────┘
  (?debug=1일 때만) [FailurePanel] 장애 5종 재현 버튼
```

- 값·단위·출처·조회 시각이 스크롤 없이 한 화면에 보이도록 `CurrentValueCard`를 최상단에 고정한다 (완료 기준 1).
- `sourceUrl`은 `<a target="_blank" rel="noreferrer">`로, 클릭 한 번에 원자료 페이지가 새 탭으로 열린다 (완료 기준 2).

---

## 9. 카드별 구현 순서와 통과 기준

| 카드 | 만드는 것 | 완료 판정 |
|------|-----------|-----------|
| 1 · 값의 맥락 | `providers/`, `fetchSnapshot()`, `CurrentValueCard` | 현재값·단위·출처·조회 시각이 한 화면에 보인다 |
| 2 · 비밀키와 호출 경로 | 공급자 선택(§2) 또는 프록시(§5), 비밀값 4곳 검사 | 출처 링크가 보이고 클릭하면 원자료가 열린다, 비밀값 0건 |
| 3 · 실패 구분 | `dashboardState.js`, `failureSim.js`, `FailurePanel` | 장애 5종이 서로 다른 문구로 보이고 정상값은 유지된다 |
| 4 · 하루 한 번 기록 | `timezone.js`, `storage/history.js`, `HistoryList` | 같은 날 재실행해도 중복 없이 날짜별 1건만 남는다 |
| 5 · 어제와 비교 | `computeDiff.js`, `DiffCard`, 대조표 | 차이·방향·단위가 한 화면에 보이고 손계산과 일치한다 |

카드 순서 = 구현 순서. 3번(상태 분리)을 건너뛰고 4번부터 만들면, 장애 상황에서 잘못된 값이 그대로 저장되는 결함이 나중에 발견하기 훨씬 어려워진다.

---

## 10. 검증 안내서 골격

> **어디로 가나요** — `https://<배포주소>` (설치·로그인 없음)
>
> **무엇을 하나요** (3단계)
> 1. 화면 상단 현재값·출처·조회 시각을 확인한다.
> 2. 출처 링크를 눌러 원자료 페이지가 열리는지 확인한다.
> 3. 날짜별 기록 목록에서 어제 대비 변화 표시를 확인한다.
>
> **무엇이 보이면 통과인가요** — 값·단위·출처·조회 시각이 한 화면에 있고, 출처 링크가 원자료를 열며, 날짜별 기록에 중복 없이 최소 2건이 있고, 그 사이 차이·방향·단위가 보인다.
>
> **안 될 때** — 값이 안 보이면 새로고침 후 "다시 시도"를 누른다. 출처 링크가 안 열리면 브라우저 팝업 차단을 해제한다. 어제 대비 비교가 안 보이면 아직 서로 다른 날짜 기록이 2건 미만인 것이다.

---

## 11. 미리 대비할 위험

| 위험 | 대응 |
|------|------|
| 자정 근처 실행 시 UTC 기준으로 날짜가 하루 밀림 | `dateKeyOf()`에서 반드시 `timeZone: 'Asia/Seoul'` 명시, 로컬 시스템 타임존에 의존하지 않음 |
| 키가 필요한 API를 나중에 고름 → 카드 2를 뒤늦게 다시 해야 함 | §2 후보 비교표에서 "키 불필요"를 1순위로 미리 확정 |
| 장애 시뮬레이션 버튼이 배포 화면에 그대로 노출 | `FailurePanel`은 `?debug=1` 쿼리로만 렌더링, 기본 배포 화면에는 없음 |
| localStorage가 브라우저별로 분리돼 다른 기기에서 기록이 안 보임 | "개인 정보판"이므로 설계상 허용 — 검증 안내서에 "같은 브라우저에서 확인" 명시 |
| `attempt` 실패 처리 중 실수로 `lastGood`을 같이 초기화 | `fetchSnapshot()` 내부에서 `lastGood` 갱신은 성공 분기 단 한 곳에서만 |
| 반올림을 저장 시점에 해버려 손계산과 화면값이 어긋남 | 저장값은 원본 정밀도 유지, 반올림은 화면 렌더링 직전 1회만 |
| 부동소수점 오차로 "0.0인데 증가" 같은 화살표·숫자 불일치 | 변화 **방향을 반올림한 값에서** 뽑는다. 실측: `26.4 − 25.2 = 1.1999999999999993` |
| 예보값을 관측값처럼 저장 | `hourly.time > current.time`인 항목 제외 (§6) |
| 지난 날짜를 채워 넣고 오늘 조회한 것처럼 보임 | 기록마다 `origin` 배지로 구분 (§6) |
